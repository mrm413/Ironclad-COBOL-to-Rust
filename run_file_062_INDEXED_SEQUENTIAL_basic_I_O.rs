// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct FilexRec {
    /// FILEX-KEY
    pub filex_key: FixedString<6>,
    /// FILEX-DATA
    pub filex_data: FixedString<10>,
}
impl std::fmt::Display for FilexRec {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.filex_key, self.filex_data)
    }
}
impl From<&str> for FilexRec {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for FilexRec {
    fn from(_s: String) -> Self { Self::default() }
}
impl FilexRec {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Filex {
    /// FILEX-REC
    pub filex_rec: FilexRec,
}
impl std::fmt::Display for Filex {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.filex_rec)
    }
}
impl From<&str> for Filex {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Filex {
    fn from(_s: String) -> Self { Self::default() }
}
impl Filex {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD group: FILEX-REC
    pub filex_rec: FixedString<16>,
    /// FD: FILEX-KEY
    pub filex_key: FixedString<6>,
    /// FD: FILEX-DATA
    pub filex_data: FixedString<10>,
    /// File status for FILEX
    pub _fs_filex: FileStatus,
    /// File handle for FILEX
    pub _fh_filex: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            filex_rec: Default::default(),
            filex_key: Default::default(),
            filex_data: Default::default(),
            _fs_filex: FileStatus::Success,
            _fh_filex: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// FILEX
fn p_filex(state: &mut ProgramState) {
    state.filex_rec = Default::default();
    filex_rec_write(state);
    filex_close(state);
    filex_open_input(state);
    filex_read(state);
    // SKIP: unresolved condition
    println!("{}", "FAILED");
    filex_close(state);
    { let _ = state._fh_filex.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT FILEX
fn filex_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILEX").unwrap_or("filex.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_filex = handle; state._fs_filex = FileStatus::Success; }
        Err(_) => { state._fs_filex = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT FILEX
fn filex_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILEX").unwrap_or("filex.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_filex = handle; state._fs_filex = FileStatus::Success; }
        Err(_) => { state._fs_filex = FileStatus::Error("35".to_string()); }
    }
}

/// READ FILEX
fn filex_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_filex.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(16);
            if _len > 0 { let _end = _len.min(0+6); state.filex_key = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 6 { let _end = _len.min(6+10); state.filex_data = FixedString::from(String::from_utf8_lossy(&_bytes[6.._end])); }
            state._fs_filex = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_filex = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_filex = e.clone();
        }
    }
    Ok(())
}

/// WRITE to FILEX
fn filex_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 16];
    { let _src = state.filex_key.as_bytes(); let _end = (0+6).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.filex_data.as_bytes(); let _end = (6+10).min(_buf.len()); let _cl = _src.len().min(_end-6); _buf[6..6+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_filex.write_record(&_buf) {
        Ok(()) => state._fs_filex = FileStatus::Success,
        Err(e) => state._fs_filex = e,
    }
}

/// CLOSE FILEX
fn filex_close(state: &mut ProgramState) {
    match state._fh_filex.close() {
        Ok(()) => state._fs_filex = FileStatus::Success,
        Err(e) => state._fs_filex = e,
    }
}

/// OPEN I-O FILEX
fn filex_open_io(state: &mut ProgramState) {
    let path = std::env::var("FILEX").unwrap_or("filex.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_filex = handle; state._fs_filex = FileStatus::Success; }
        Err(_) => { state._fs_filex = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND FILEX
fn filex_open_extend(state: &mut ProgramState) {
    let path = std::env::var("FILEX").unwrap_or("filex.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_filex = handle; state._fs_filex = FileStatus::Success; }
        Err(_) => { state._fs_filex = FileStatus::Error("35".to_string()); }
    }
}

/// WRITE FILEX-REC
fn filex_rec_write(state: &mut ProgramState) {
    // Write FILEX-REC via filex_write (byte buffer)
    filex_write(state);
}

fn main() {
    let mut state = ProgramState::default();
    p_filex(&mut state);
}

