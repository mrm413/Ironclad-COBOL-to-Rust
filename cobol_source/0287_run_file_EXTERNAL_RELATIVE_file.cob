// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct MyRecord {
    /// MY-RECORD-DATA
    pub my_record_data: FixedString<80>,
}

#[derive(Debug, Clone, Default)]
pub struct MyFile {
    /// MY-RECORD
    pub my_record: MyRecord,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD group: MY-RECORD
    pub my_record: FixedString<80>,
    /// FD: MY-RECORD-DATA
    pub my_record_data: FixedString<80>,
    /// MY-KEY
    pub my_key: u32,
    /// File status for MY-FILE
    pub my_file_status: FileStatus,
    /// File handle for MY-FILE
    pub my_file_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            my_record: Default::default(),
            my_record_data: Default::default(),
            my_key: Default::default(),
            my_file_status: FileStatus::Success,
            my_file_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT MY-FILE
fn my_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("MY_FILE").unwrap_or("my_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.my_file_handle = handle; state.my_file_status = FileStatus::Success; }
        Err(_) => { state.my_file_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT MY-FILE
fn my_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("MY_FILE").unwrap_or("my_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.my_file_handle = handle; state.my_file_status = FileStatus::Success; }
        Err(_) => { state.my_file_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ MY-FILE
fn my_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.my_file_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(80);
            if _len > 0 { let _end = _len.min(0+80); state.my_record = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 0 { let _end = _len.min(0+80); state.my_record_data = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.my_file_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.my_file_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.my_file_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to MY-FILE
fn my_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 80];
    { let _src = state.my_record_data.as_bytes(); let _end = (0+80).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.my_file_handle.write_record(&_buf) {
        Ok(()) => state.my_file_status = FileStatus::Success,
        Err(e) => state.my_file_status = e,
    }
}

/// CLOSE MY-FILE
fn my_file_close(state: &mut ProgramState) {
    match state.my_file_handle.close() {
        Ok(()) => state.my_file_status = FileStatus::Success,
        Err(e) => state.my_file_status = e,
    }
}

fn main() {
    // No paragraphs found
}

