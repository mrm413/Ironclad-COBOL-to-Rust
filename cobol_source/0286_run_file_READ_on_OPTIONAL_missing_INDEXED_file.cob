// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct File1 {
    /// FILE1-KEY
    pub file1_key: FixedString<1>,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: FILE1-KEY
    pub file1_key: FixedString<1>,
    /// F-STATUS
    pub f_status: FixedString<2>,
    /// File status for FILE1
    pub file1_status: FileStatus,
    /// File handle for FILE1
    pub file1_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            file1_key: Default::default(),
            f_status: Default::default(),
            file1_status: FileStatus::Success,
            file1_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// FILE1
fn p_file1(state: &mut ProgramState) {
    file1_open_input(state);
    if state.? {
    }
    println!("{}{}", "FAILED OPEN: fs=", state.f_status);
    match file1_read(state) {
        Ok(()) => {
            // record read successfully
        }
        Err(FileStatus::AtEnd) => {
            if state.? {
            }
        }
        Err(_) => {
            // other file error
        }
    }
    println!("{}{}", "FAILED READ AT END: fs=", state.f_status);
    println!("{}{}", "FAILED READ NO AT END: status ", state.f_status);
    file1_close(state);
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT FILE1
fn file1_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILE1").unwrap_or("file1.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.file1_handle = handle; state.file1_status = FileStatus::Success; }
        Err(_) => { state.file1_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT FILE1
fn file1_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILE1").unwrap_or("file1.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.file1_handle = handle; state.file1_status = FileStatus::Success; }
        Err(_) => { state.file1_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ FILE1
fn file1_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.file1_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.file1_key = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.file1_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.file1_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.file1_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to FILE1
fn file1_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.file1_key.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.file1_handle.write_record(&_buf) {
        Ok(()) => state.file1_status = FileStatus::Success,
        Err(e) => state.file1_status = e,
    }
}

/// CLOSE FILE1
fn file1_close(state: &mut ProgramState) {
    match state.file1_handle.close() {
        Ok(()) => state.file1_status = FileStatus::Success,
        Err(e) => state.file1_status = e,
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_file1(&mut state);
}

