// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.


use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct Switch {
    /// FILLER PIC FILLER
    pub _filler_33: FixedString<1>,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// SWITCH
    pub switch: Switch,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            switch: "",
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// END-ADD
fn p_end_add(state: &mut ProgramState) {
    if state.switch.as_str() != 13 {
        println!("{}{}{}", "SWITCH (variable) + 1 WRONG: ", state.switch, state.end_display);
    }
    state.z += state.switch;
}

/// END-ADD
fn p_end_add(state: &mut ProgramState) {
    if state.switch.as_str() != 24 {
        println!("{}{}{}", "SWITCH (variable) + Z WRONG: ", state.switch, state.end_display);
    }
    if state.swit1_on {
        println!("{}{}{}{}", "ON", state.no, state.advancing, state.end_display);
    } else {
        println!("{}{}{}{}", "OFF", state.no, state.advancing, state.end_display);
    }
    if state.swit2_on {
        println!("{}{}{}{}", " ON", state.no, state.advancing, state.end_display);
    } else {
        println!("{}{}{}{}", " OFF", state.no, state.advancing, state.end_display);
    }
    state.switch_b = state.off;
    if state.swit2_on {
        println!("{}{}{}{}", " ON", state.no, state.advancing, state.end_display);
    } else {
        println!("{}{}{}{}", " OFF", state.no, state.advancing, state.end_display);
    }
    if state.swit25_on {
        println!("{}{}{}{}", " ON", state.no, state.advancing, state.end_display);
    } else {
        println!("{}{}{}{}", " OFF", state.no, state.advancing, state.end_display);
    }
    if state.swit26_on {
        println!("{}{}{}{}", " ON", state.no, state.advancing, state.end_display);
    } else {
        println!("{}{}{}{}", " OFF", state.no, state.advancing, state.end_display);
    }
    if state.swit31_on {
        println!("{}{}{}{}", " ON", state.no, state.advancing, state.end_display);
    } else {
        println!("{}{}{}{}", " OFF", state.no, state.advancing, state.end_display);
    }
    if state.swit32_on {
        println!("{}{}{}{}", " ON", state.no, state.advancing, state.end_display);
    } else {
        println!("{}{}{}{}", " OFF", state.no, state.advancing, state.end_display);
    }
    { std::process::exit(0); }
}

fn main() {
    let mut state = ProgramState::default();
    p_end_add(&mut state);
}

