// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct OutRec {
    /// PRIME-KEY
    pub prime_key: FixedString<25>,
    /// IN-DATA
    pub in_data: FixedString<10>,
}

#[derive(Debug, Clone, Default)]
pub struct Outfile {
    /// OUT-REC
    pub out_rec: OutRec,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD group: OUT-REC
    pub out_rec: FixedString<35>,
    /// FD: PRIME-KEY
    pub prime_key: FixedString<25>,
    /// FD: IN-DATA
    pub in_data: FixedString<10>,
    /// OUT-STAT
    pub out_stat: FixedString<2>,
    /// OUT-RECNUM
    pub out_recnum: u32,
    /// File status for OUTFILE
    pub outfile_status: FileStatus,
    /// File handle for OUTFILE
    pub outfile_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            out_rec: Default::default(),
            prime_key: Default::default(),
            in_data: Default::default(),
            out_stat: FixedString::from_str("00"),
            out_recnum: 0,
            outfile_status: FileStatus::Success,
            outfile_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// MAIN-100
fn p_main_100(state: &mut ProgramState) {
    outfile_open_output(state);
    if state.out_stat != "00" {
        println!("{}{}{}", "ERROR ", state.out_stat, " OPENING OUTPUT FILE");
        { let _ = state.outfile_handle.close();
            std::process::exit(0); }
    }
    state.out_rec = Default::default();
}

/// GET-NEXT-100
fn p_get_next_100(state: &mut ProgramState) {
    state.out_recnum += 1;
    state.prime_key = "RECORD-X".into();
    { let _s = (8 - 1) as usize; let _l = (1) as usize; let _src = state.out_recnum.as_bytes(); let _dst = state.prime_key.as_bytes_mut(); let _cl = _l.min(_src.len()).min(_dst.len() - _s); _dst[_s.._s+_cl].copy_from_slice(&_src[.._cl]); }
    out_rec_write(state);
    if (state.out_stat != "00") && (state.out_stat != "02") {
        println!("{}{}{}", "ERROR ", state.out_stat, " WRITING OUTPUT");
        p_end_prog_100(state); return;
    }
    if state.out_recnum < 5 {
        p_get_next_100(state); return;
    }
}

/// END-PROG-100
fn p_end_prog_100(state: &mut ProgramState) {
    println!("{}{}", state.out_recnum, " records written");
    outfile_close(state);
    { let _ = state.outfile_handle.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT OUTFILE
fn outfile_open_input(state: &mut ProgramState) {
    let path = std::env::var("OUTFILE").unwrap_or("outfile.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.outfile_handle = handle; state.outfile_status = FileStatus::Success; }
        Err(_) => { state.outfile_status = FileStatus::Error("35".to_string()); }
    }
    state.out_stat = match &state.outfile_status { FileStatus::Success => FixedString::from("00"), FileStatus::AtEnd => FixedString::from("10"), FileStatus::Error(c) => FixedString::from(c.as_str()), };
}

/// OPEN OUTPUT OUTFILE
fn outfile_open_output(state: &mut ProgramState) {
    let path = std::env::var("OUTFILE").unwrap_or("outfile.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.outfile_handle = handle; state.outfile_status = FileStatus::Success; }
        Err(_) => { state.outfile_status = FileStatus::Error("35".to_string()); }
    }
    state.out_stat = match &state.outfile_status { FileStatus::Success => FixedString::from("00"), FileStatus::AtEnd => FixedString::from("10"), FileStatus::Error(c) => FixedString::from(c.as_str()), };
}

/// READ OUTFILE
fn outfile_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.outfile_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(35);
            if _len > 0 { let _end = _len.min(0+35); state.out_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 0 { let _end = _len.min(0+25); state.prime_key = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 25 { let _end = _len.min(25+10); state.in_data = FixedString::from(String::from_utf8_lossy(&_bytes[25.._end])); }
            state.outfile_status = FileStatus::Success;
    state.out_stat = match &state.outfile_status { FileStatus::Success => FixedString::from("00"), FileStatus::AtEnd => FixedString::from("10"), FileStatus::Error(c) => FixedString::from(c.as_str()), };
        }
        Err(FileStatus::AtEnd) => {
            state.outfile_status = FileStatus::AtEnd;
    state.out_stat = match &state.outfile_status { FileStatus::Success => FixedString::from("00"), FileStatus::AtEnd => FixedString::from("10"), FileStatus::Error(c) => FixedString::from(c.as_str()), };
        }
        Err(e) => {
            state.outfile_status = e.clone();
    state.out_stat = match &state.outfile_status { FileStatus::Success => FixedString::from("00"), FileStatus::AtEnd => FixedString::from("10"), FileStatus::Error(c) => FixedString::from(c.as_str()), };
        }
    }
    Ok(())
}

/// WRITE to OUTFILE
fn outfile_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 35];
    { let _src = state.prime_key.as_bytes(); let _end = (0+25).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.in_data.as_bytes(); let _end = (25+10).min(_buf.len()); let _cl = _src.len().min(_end-25); _buf[25..25+_cl].copy_from_slice(&_src[.._cl]); }
    match state.outfile_handle.write_record(&_buf) {
        Ok(()) => state.outfile_status = FileStatus::Success,
        Err(e) => state.outfile_status = e,
    }
}

/// CLOSE OUTFILE
fn outfile_close(state: &mut ProgramState) {
    match state.outfile_handle.close() {
        Ok(()) => state.outfile_status = FileStatus::Success,
        Err(e) => state.outfile_status = e,
    }
    state.out_stat = match &state.outfile_status { FileStatus::Success => FixedString::from("00"), FileStatus::AtEnd => FixedString::from("10"), FileStatus::Error(c) => FixedString::from(c.as_str()), };
}

/// WRITE OUT-REC
fn out_rec_write(state: &mut ProgramState) {
    // Write OUT-REC via outfile_write (byte buffer)
    outfile_write(state);
}

fn main() {
    let mut state = ProgramState::default();
    p_main_100(&mut state);
}

