// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct File1 {
    /// FILE1-REC
    pub file1_rec: FixedString<1>,
}

#[derive(Debug, Clone, Default)]
pub struct File2 {
    /// FILE2-REC
    pub file2_rec: FixedString<2>,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: FILE1-REC
    pub file1_rec: FixedString<1>,
    /// FD: FILE2-REC
    pub file2_rec: FixedString<2>,
    /// File status for FILE1
    pub file1_status: FileStatus,
    /// File handle for FILE1
    pub file1_handle: CobolFile,
    /// File status for FILE2
    pub file2_status: FileStatus,
    /// File handle for FILE2
    pub file2_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            file1_rec: Default::default(),
            file2_rec: Default::default(),
            file1_status: FileStatus::Success,
            file1_handle: CobolFile::Closed,
            file2_status: FileStatus::Success,
            file2_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// FILE2
fn p_file2(state: &mut ProgramState) {
    { let _ = state.file1_handle.close();
        let _ = state.file2_handle.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT FILE1
fn file1_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILE1").unwrap_or("file1.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.file1_handle = handle; state.file1_status = FileStatus::Success; }
        Err(_) => { state.file1_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT FILE1
fn file1_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILE1").unwrap_or("file1.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.file1_handle = handle; state.file1_status = FileStatus::Success; }
        Err(_) => { state.file1_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ FILE1
fn file1_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.file1_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.file1_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.file1_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.file1_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.file1_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to FILE1
fn file1_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.file1_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.file1_handle.write_record(&_buf) {
        Ok(()) => state.file1_status = FileStatus::Success,
        Err(e) => state.file1_status = e,
    }
}

/// CLOSE FILE1
fn file1_close(state: &mut ProgramState) {
    match state.file1_handle.close() {
        Ok(()) => state.file1_status = FileStatus::Success,
        Err(e) => state.file1_status = e,
    }
}

/// OPEN INPUT FILE2
fn file2_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILE2").unwrap_or("file2.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.file2_handle = handle; state.file2_status = FileStatus::Success; }
        Err(_) => { state.file2_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT FILE2
fn file2_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILE2").unwrap_or("file2.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.file2_handle = handle; state.file2_status = FileStatus::Success; }
        Err(_) => { state.file2_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ FILE2
fn file2_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.file2_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(2);
            if _len > 0 { let _end = _len.min(0+2); state.file2_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.file2_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.file2_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.file2_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to FILE2
fn file2_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 2];
    { let _src = state.file2_rec.as_bytes(); let _end = (0+2).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.file2_handle.write_record(&_buf) {
        Ok(()) => state.file2_status = FileStatus::Success,
        Err(e) => state.file2_status = e,
    }
}

/// CLOSE FILE2
fn file2_close(state: &mut ProgramState) {
    match state.file2_handle.close() {
        Ok(()) => state.file2_status = FileStatus::Success,
        Err(e) => state.file2_status = e,
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_file2(&mut state);
}

