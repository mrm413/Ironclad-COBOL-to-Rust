// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct F {
    /// F-REC
    pub f_rec: FixedString<100>,
}

#[derive(Debug, Clone, Default)]
pub struct G {
    /// G-REC
    pub g_rec: u64,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: F-REC
    pub f_rec: FixedString<100>,
    /// FD: G-REC
    pub g_rec: u64,
    /// CORE-IDX
    pub core_idx: u32,
    /// F-MAX
    pub f_max: u32,
    /// G-KEY
    pub g_key: u32,
    /// G-ACTUAL-KEY
    pub g_actual_key: FixedString<3>,
    /// File status for F
    pub f_status: FileStatus,
    /// File handle for F
    pub f_handle: CobolFile,
    /// File status for G
    pub g_status: FileStatus,
    /// File handle for G
    pub g_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            f_rec: Default::default(),
            g_rec: Default::default(),
            core_idx: Default::default(),
            f_max: 1000,
            g_key: Default::default(),
            g_actual_key: Default::default(),
            f_status: FileStatus::Success,
            f_handle: CobolFile::Closed,
            g_status: FileStatus::Success,
            g_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// REREAD
fn p_reread(state: &mut ProgramState) {
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT F
fn f_open_input(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.f_handle = handle; state.f_status = FileStatus::Success; }
        Err(_) => { state.f_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT F
fn f_open_output(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.f_handle = handle; state.f_status = FileStatus::Success; }
        Err(_) => { state.f_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ F
fn f_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.f_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+100); state.f_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.f_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.f_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.f_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to F
fn f_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.f_rec.as_bytes(); let _end = (0+100).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.f_handle.write_record(&_buf) {
        Ok(()) => state.f_status = FileStatus::Success,
        Err(e) => state.f_status = e,
    }
}

/// CLOSE F
fn f_close(state: &mut ProgramState) {
    match state.f_handle.close() {
        Ok(()) => state.f_status = FileStatus::Success,
        Err(e) => state.f_status = e,
    }
}

/// OPEN INPUT G
fn g_open_input(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.g_handle = handle; state.g_status = FileStatus::Success; }
        Err(_) => { state.g_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT G
fn g_open_output(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.g_handle = handle; state.g_status = FileStatus::Success; }
        Err(_) => { state.g_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ G
fn g_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.g_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(10);
            if _len > 0 { let _end = _len.min(0+10); let _s = String::from_utf8_lossy(&_bytes[0.._end]).trim().to_string(); state.g_rec = _s.parse().unwrap_or_default(); }
            state.g_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.g_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.g_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to G
fn g_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 10];
    { let _s = format!("{:>width$}", state.g_rec, width = 10); let _b = _s.as_bytes(); let _cl = _b.len().min(10); _buf[0..0+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    match state.g_handle.write_record(&_buf) {
        Ok(()) => state.g_status = FileStatus::Success,
        Err(e) => state.g_status = e,
    }
}

/// CLOSE G
fn g_close(state: &mut ProgramState) {
    match state.g_handle.close() {
        Ok(()) => state.g_status = FileStatus::Success,
        Err(e) => state.g_status = e,
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_reread(&mut state);
}

