// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct File1Rec {
    /// FILE1-SERIAL-1
    pub file1_serial_1: u32,
    /// FILE1-XSEEN
    pub file1_xseen: u32,
    /// FILE1-SERIAL-2
    pub file1_serial_2: u32,
}

#[derive(Debug, Clone, Default)]
pub struct File1 {
    /// FILE1-REC
    pub file1_rec: File1Rec,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD group: FILE1-REC
    pub file1_rec: FixedString<12>,
    /// FD: FILE1-SERIAL-1
    pub file1_serial_1: u32,
    /// FD: FILE1-XSEEN
    pub file1_xseen: u32,
    /// FD: FILE1-SERIAL-2
    pub file1_serial_2: u32,
    /// W-COUNT
    pub w_count: u32,
    /// W-EOF
    pub w_eof: u32,
    /// W-ABORT
    pub w_abort: u32,
    /// File status for FILE1
    pub file1_status: FileStatus,
    /// File handle for FILE1
    pub file1_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            file1_rec: Default::default(),
            file1_serial_1: Default::default(),
            file1_xseen: Default::default(),
            file1_serial_2: Default::default(),
            w_count: 0,
            w_eof: 0,
            w_abort: 0,
            file1_status: FileStatus::Success,
            file1_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// FILE1
fn p_file1(state: &mut ProgramState) {
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT FILE1
fn file1_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILE1").unwrap_or("file1.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.file1_handle = handle; state.file1_status = FileStatus::Success; }
        Err(_) => { state.file1_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT FILE1
fn file1_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILE1").unwrap_or("file1.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.file1_handle = handle; state.file1_status = FileStatus::Success; }
        Err(_) => { state.file1_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ FILE1
fn file1_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.file1_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(16);
            if _len > 0 { let _end = _len.min(0+16); state.file1_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 0 { let _end = _len.min(0+6); let _s = String::from_utf8_lossy(&_bytes[0.._end]).trim().to_string(); state.file1_serial_1 = _s.parse().unwrap_or_default(); }
            if _len > 6 { let _end = _len.min(6+4); let _s = String::from_utf8_lossy(&_bytes[6.._end]).trim().to_string(); state.file1_xseen = _s.parse().unwrap_or_default(); }
            if _len > 10 { let _end = _len.min(10+6); let _s = String::from_utf8_lossy(&_bytes[10.._end]).trim().to_string(); state.file1_serial_2 = _s.parse().unwrap_or_default(); }
            state.file1_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.file1_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.file1_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to FILE1
fn file1_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 16];
    { let _s = format!("{:>width$}", state.file1_serial_1, width = 6); let _b = _s.as_bytes(); let _cl = _b.len().min(6); _buf[0..0+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.file1_xseen, width = 4); let _b = _s.as_bytes(); let _cl = _b.len().min(4); _buf[6..6+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.file1_serial_2, width = 6); let _b = _s.as_bytes(); let _cl = _b.len().min(6); _buf[10..10+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    match state.file1_handle.write_record(&_buf) {
        Ok(()) => state.file1_status = FileStatus::Success,
        Err(e) => state.file1_status = e,
    }
}

/// CLOSE FILE1
fn file1_close(state: &mut ProgramState) {
    match state.file1_handle.close() {
        Ok(()) => state.file1_status = FileStatus::Success,
        Err(e) => state.file1_status = e,
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_file1(&mut state);
}

