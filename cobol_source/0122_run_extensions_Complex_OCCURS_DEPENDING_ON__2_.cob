// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.


use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct Vtop {
    /// VGROUP
    pub vgroup: Vgroup,
}

#[derive(Debug, Clone, Default)]
pub struct Vgroup {
    /// VX
    pub vx: [Vx; 1],
    /// VY
    pub vy: [Vy; 1],
    /// ZZ
    pub zz: [Zz; 1],
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// I
    pub i: u32,
    /// J
    pub j: u32,
    /// K
    pub k: u32,
    /// VLEN
    pub vlen: u32,
    /// VTOP
    pub vtop: Vtop,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            i: Default::default(),
            j: Default::default(),
            k: Default::default(),
            vlen: Default::default(),
            vtop: Default::default(),
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// I
fn p_i(state: &mut ProgramState) {
    state.j = 1;
    state.k = 1;
    state.vtop.vgroup.vx.vxx[(1 - 1) as usize] = "1".into();
    state.vtop.vgroup.vy.vyy[(1 - 1) as usize] = "22".into();
    state.vtop.vgroup.zz.vzz[(1 - 1) as usize] = "333".into();
    state.vlen = std::mem::size_of_val(&state.vtop.vgroup) as i32;
    println!("{}{}{}{}", state.vlen, state.no, state.advancing, state.end_display);
    state.vtop.vgroup = Default::default();
    state.?missing? = std::mem::size_of_val(&state.?missing?) as i32;
    state.to = std::mem::size_of_val(&state.?missing?) as i32;
    state.vlen = std::mem::size_of_val(&state.?missing?) as i32;
    println!("{}{}{}{}", state.vlen, state.no, state.advancing, state.end_display);
    println!("{}{}", state.vtop.vgroup, state.end_display);
    state.vtop = Default::default();
    state.i = 3;
    state.j = 2;
    state.k = 1;
    state.vtop.vgroup.vx.vxx[(3 - 1) as usize] = "3".into();
    state.vtop.vgroup.vy.vyy[(2 - 1) as usize] = "22".into();
    state.vtop.vgroup.zz.vzz[(1 - 1) as usize] = "111".into();
    state.vlen = std::mem::size_of_val(&state.vtop.vgroup) as i32;
    println!("{}{}{}{}", state.vlen, state.no, state.advancing, state.end_display);
    state.vtop.vgroup = Default::default();
    state.?missing? = std::mem::size_of_val(&state.?missing?) as i32;
    state.to = std::mem::size_of_val(&state.?missing?) as i32;
    state.vlen = std::mem::size_of_val(&state.?missing?) as i32;
    println!("{}{}{}{}", state.vlen, state.no, state.advancing, state.end_display);
    println!("{}{}", state.vtop.vgroup, state.end_display);
    { std::process::exit(0); }
}

fn main() {
    let mut state = ProgramState::default();
    p_i(&mut state);
}

