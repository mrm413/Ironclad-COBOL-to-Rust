// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.


use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone)]
pub struct ProgramState {
    /// SWITCH
    pub switch: u32,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            switch: "",
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// END-ADD
fn p_end_add(state: &mut ProgramState) {
    if state.switch != 13 {
        println!("{}{}{}", "SWITCH (variable) WRONG: ", state.switch, state.end_display);
    }
    if state.swit1_on {
        println!("{}{}", "ON", state.end_display);
    } else {
        println!("{}{}", "OFF", state.end_display);
    }
    if state.swit2_on {
        println!("{}{}", " ON", state.end_display);
    } else {
        println!("{}{}", " OFF", state.end_display);
    }
    state.switch_b = state.off;
    if state.swit2_on {
    }
    if state.swit25_on {
    }
    if state.swit26_on {
    }
    if state.swit32_on {
    }
    if state.swit32_off {
    }
    { std::process::exit(0); }
}

fn main() {
    let mut state = ProgramState::default();
    p_end_add(&mut state);
}

