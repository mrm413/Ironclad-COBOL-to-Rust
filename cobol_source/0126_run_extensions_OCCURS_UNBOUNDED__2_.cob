// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.


use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct Vargrp {
    /// OBJ
    pub obj: u32,
    /// TABGRP
    pub tabgrp: Tabgrp,
}

#[derive(Debug, Clone, Default)]
pub struct Tabgrp {
    /// VARTAB
    pub vartab: [Vartab; 1],
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// X
    pub x: PackedDecimal<2> /* Unsigned */,
    /// NUM-ELEMENTS
    pub num_elements: u32,
    /// SIZE-NEEDED
    pub size_needed: u32,
    /// OLD-SIZE
    pub old_size: u32,
    /// VPTR
    pub vptr: FixedString<1>,
    /// LINKAGE: VARGRP
    pub vargrp: Vargrp,
    /// LINKAGE: BUFFER
    pub buffer: FixedString<1000>,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            x: Default::default(),
            num_elements: Default::default(),
            size_needed: Default::default(),
            old_size: Default::default(),
            vptr: Default::default(),
            vargrp: Default::default(),
            buffer: Default::default(),
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// ALLOC-VARGRP
fn p_alloc_vargrp(state: &mut ProgramState) {
    if state.vptr == state.null {
        state.size_needed = std::mem::size_of_val(&state.vargrp.obj) as i32 + std::mem::size_of_val(&state.vargrp.tabgrp.vartab) as i32 * state.num_elements;
        println!("{}{}{}{}{}{}{}{}{}", "First allocation, using ", state.size_needed, " bytes.", state.allocate, state.size_needed, state.characters, state.initialized, state.returning, state.vptr);
        state.address = state.of;
    }
    state.vargrp.obj = state.num_elements;
    state.old_size = state.size_needed;
    state.address = state.of;
    state.size_needed = std::mem::size_of_val(&state.vargrp.obj) as i32 + std::mem::size_of_val(&state.vargrp.tabgrp.vartab) as i32 * state.num_elements;
    println!("{}{}{}{}", "Re-allocation, using ", state.size_needed, " bytes.", "?MISSING?");
    if state.size_needed < 2097152 {
    }
    state.address = state.of;
    state.vargrp.obj = state.num_elements;
    {
        let _bytes = state.buffer.as_bytes();
        let _len = _bytes.len();
        if _len > 0 { let _end = _len.min(0+2); let _s = String::from_utf8_lossy(&_bytes[0.._end]).trim().to_string(); state.vargrp.obj = _s.parse().unwrap_or_default(); }
        if _len > 2 { let _end = _len.min(2+4); let _s = String::from_utf8_lossy(&_bytes[2.._end]).trim().to_string(); state.vargrp.tabgrp.vartab.t1 = _s.parse().unwrap_or_default(); }
        if _len > 6 { let _end = _len.min(6+8); state.vargrp.tabgrp.vartab.t2 = FixedString::from(String::from_utf8_lossy(&_bytes[6.._end])); }
        if _len > 14 { let _end = _len.min(14+4); let _s = String::from_utf8_lossy(&_bytes[14.._end]).trim().to_string(); state.vargrp.tabgrp.vartab.t3 = _s.parse().unwrap_or_default(); }
    }
    state.old_size = state.size_needed;
    state.?missing? = state.size_needed;
    state.?missing? = state.size_needed;
    state.?missing? = state.size_needed;
    state.vptr = &state.buffer as *const _ as usize;
}

/// VPTR
fn p_vptr(state: &mut ProgramState) {
}

fn main() {
    let mut state = ProgramState::default();
    p_alloc_vargrp(&mut state);
}

