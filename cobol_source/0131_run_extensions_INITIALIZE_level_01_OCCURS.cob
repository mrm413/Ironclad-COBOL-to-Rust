// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.


use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone)]
pub struct ProgramState {
    /// L1
    pub l1: [L1; 1000],
    /// INIT
    pub init: [FixedString<256>; 3],
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            l1: Default::default(),
            init: Default::default(),
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// DEFAULT
fn p_default(state: &mut ProgramState) {
    if state.l1.l2[(1 - 1) as usize] != 0 {
        println!("{}{}", "2 VALUE(1) = ", state.l1.l2[(1 - 1) as usize]);
        if state.l1.l2[(3 - 1) as usize] != 5 {
            println!("{}{}", "2 VALUE(3) = ", state.l1.l2[(3 - 1) as usize]);
            state.l1[((1 - 1) as usize).min(999)] = Default::default();
            state.all = Default::default();
            state.value = Default::default();
            if state.l1.l2[(1 - 1) as usize] != 5 {
                println!("{}{}", "3 VALUE(1) = ", state.l1.l2[(1 - 1) as usize]);
                if state.l1.l2[(3 - 1) as usize] != 5 {
                    println!("{}{}", "3 VALUE(3) = ", state.l1.l2[(3 - 1) as usize]);
                    if state.init[((1 - 1) as usize).min(2)] != FixedString::spaces() {
                        println!("{}", "INIT1 NOT = SPACES");
                        if state.init[((3 - 1) as usize).min(2)] != FixedString::spaces() {
                            println!("{}", "INIT3 NOT = SPACES");
                            state.?missing? = state.all;
                            state.to = state.all;
                            state.init[((1 - 1) as usize).min(2)] = state.all;
                            state.init[((2 - 1) as usize).min(2)] = state.all;
                            state.init[((3 - 1) as usize).min(2)] = state.all;
                            if std::str::from_utf8(&state.init[((2 - 1) as usize).min(2)].as_bytes()[(50 - 1) as usize..(50 - 1 + 5) as usize]).unwrap_or("") != "*****" {
                                println!("{}", "INIT2 BADVAL");
                                state.init[((2 - 1) as usize).min(2)] = Default::default();
                                state.all = Default::default();
                                state.to = Default::default();
                                state.value = Default::default();
                                if state.init[((2 - 1) as usize).min(2)] != FixedString::spaces() {
                                    println!("{}", "INIT2 NOT = SPACES");
                                    return;
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_default(&mut state);
}

