// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct File01 {
    /// RECORD-FILE01
    pub record_file01: FixedString<50>,
}

#[derive(Debug, Clone, Default)]
pub struct WData01 {
    /// W-CH01
    pub w_ch01: FixedString<10>,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: RECORD-FILE01
    pub record_file01: FixedString<50>,
    /// W-DATA01
    pub w_data01: WData01,
    /// File status for FILE01
    pub file01_status: FileStatus,
    /// File handle for FILE01
    pub file01_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            record_file01: Default::default(),
            w_data01: Default::default(),
            file01_status: FileStatus::Success,
            file01_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// MAIN-PROCEDURE
fn p_main_procedure(state: &mut ProgramState) {
    println!("{}", "Hello");
    { let _ = state.file01_handle.close();
        std::process::exit(0); }
}

/// PGM1
fn p_pgm1(state: &mut ProgramState) {
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT FILE01
fn file01_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILE01").unwrap_or("file01.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.file01_handle = handle; state.file01_status = FileStatus::Success; }
        Err(_) => { state.file01_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT FILE01
fn file01_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILE01").unwrap_or("file01.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.file01_handle = handle; state.file01_status = FileStatus::Success; }
        Err(_) => { state.file01_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ FILE01
fn file01_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.file01_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(50);
            if _len > 0 { let _end = _len.min(0+50); state.record_file01 = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.file01_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.file01_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.file01_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to FILE01
fn file01_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 50];
    { let _src = state.record_file01.as_bytes(); let _end = (0+50).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.file01_handle.write_record(&_buf) {
        Ok(()) => state.file01_status = FileStatus::Success,
        Err(e) => state.file01_status = e,
    }
}

/// CLOSE FILE01
fn file01_close(state: &mut ProgramState) {
    match state.file01_handle.close() {
        Ok(()) => state.file01_status = FileStatus::Success,
        Err(e) => state.file01_status = e,
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_main_procedure(&mut state);
}

