// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct TestFile {
    /// TEST-REC
    pub test_rec: FixedString<4>,
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: TEST-REC
    pub test_rec: FixedString<4>,
    /// TESTKEY
    pub testkey: FixedString<1>,
    /// TESTSTAT
    pub teststat: FixedString<2>,
    /// File status for TEST-FILE
    pub test_file_status: FileStatus,
    /// File handle for TEST-FILE
    pub test_file_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            test_rec: Default::default(),
            testkey: Default::default(),
            teststat: Default::default(),
            test_file_status: FileStatus::Success,
            test_file_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// TEST-FILE
fn p_test_file(state: &mut ProgramState) {
    if !(state.teststat == "00") {
        println!("{}{}{}", "OPEN ", state.teststat, state.end_display);
        { let _ = state.test_file_handle.close();
            return; }
    }
    state.testkey = 3;
    state.test_rec = "0003".into();
    test_rec_write(state);
    println!("{}{}{}{}", "WRITE ", state.teststat, state.end_display, state.end_write);
    state.testkey = 2;
    state.test_rec = "0002".into();
    test_rec_write(state);
    println!("{}{}{}{}", "WRITE ", state.teststat, state.end_display, state.end_write);
    state.testkey = 99;
    // START TEST-FILE
    if !(state.teststat == "00") {
        println!("{}{}{}", "START ", state.teststat, state.end_display);
    }
    if state.testkey != 99 {
        println!("{}{}{}", "TESTKEY ", state.testkey, state.end_display);
    }
    state.test_rec = FixedString::spaces();
    test_file_read(state);
    if !(state.teststat == "00") {
        println!("{}{}{}", "READ ", state.teststat, state.end_display);
    }
    if state.test_rec != "0003" {
        println!("{}{}{}", "READ RECORD ", state.test_rec, state.end_display);
    }
    test_file_close(state);
}

/// PROG
fn p_prog(state: &mut ProgramState) {
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT TEST-FILE
fn test_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("TEST_FILE").unwrap_or("test_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.test_file_handle = handle; state.test_file_status = FileStatus::Success; }
        Err(_) => { state.test_file_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT TEST-FILE
fn test_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("TEST_FILE").unwrap_or("test_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.test_file_handle = handle; state.test_file_status = FileStatus::Success; }
        Err(_) => { state.test_file_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ TEST-FILE
fn test_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.test_file_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(4);
            if _len > 0 { let _end = _len.min(0+4); state.test_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.test_file_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.test_file_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.test_file_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to TEST-FILE
fn test_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 4];
    { let _src = state.test_rec.as_bytes(); let _end = (0+4).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.test_file_handle.write_record(&_buf) {
        Ok(()) => state.test_file_status = FileStatus::Success,
        Err(e) => state.test_file_status = e,
    }
}

/// CLOSE TEST-FILE
fn test_file_close(state: &mut ProgramState) {
    match state.test_file_handle.close() {
        Ok(()) => state.test_file_status = FileStatus::Success,
        Err(e) => state.test_file_status = e,
    }
}

/// WRITE TEST-REC
fn test_rec_write(state: &mut ProgramState) {
    // Write TEST-REC via test_file_write (byte buffer)
    test_file_write(state);
}

fn main() {
    let mut state = ProgramState::default();
    p_test_file(&mut state);
}

