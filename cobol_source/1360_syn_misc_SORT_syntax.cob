// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;


#[derive(Debug, Clone, Default)]
pub struct Srtfil {
    /// SRTREC
    pub srtrec: FixedString<256>,
}

#[derive(Debug, Clone, Default)]
pub struct Stfile {
    /// STFREC
    pub stfrec: FixedString<256>,
}

#[derive(Debug, Clone, Default)]
pub struct G {
    /// TBL
    pub tbl: [Tbl; 5],
    /// TBL-ORD
    pub tbl_ord: [TblOrd; 5],
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: SRTREC
    pub srtrec: FixedString<256>,
    /// FD: STFREC
    pub stfrec: FixedString<256>,
    /// G
    pub g: G,
    /// File status for SRTFIL
    pub srtfil_status: FileStatus,
    /// File handle for SRTFIL
    pub srtfil_handle: CobolFile,
    /// File status for STFILE
    pub stfile_status: FileStatus,
    /// File handle for STFILE
    pub stfile_handle: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            srtrec: Default::default(),
            stfrec: Default::default(),
            g: "d4b2e1a3c5",
            srtfil_status: FileStatus::Success,
            srtfil_handle: CobolFile::Closed,
            stfile_status: FileStatus::Success,
            stfile_handle: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
        }
    }
}

/// X
fn p_x(state: &mut ProgramState) {
    // // SORT TBL ON 
    // // SORT TBL ON 
    // // SORT TBL-ORD ON 
    // // SORT TBL-ORD ON 
    // // SORT STFILE ON DESC SRTREC
    // // SORT STFILE ON DESC SRTREC USING SRTFIL GIVING SRTFIL
    // // SORT STFILE ON 
    // // SORT STFILE ON 
    { let _ = state.srtfil_handle.close();
        let _ = state.stfile_handle.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT SRTFIL
fn srtfil_open_input(state: &mut ProgramState) {
    let path = std::env::var("SRTFIL").unwrap_or("srtfil.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.srtfil_handle = handle; state.srtfil_status = FileStatus::Success; }
        Err(_) => { state.srtfil_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT SRTFIL
fn srtfil_open_output(state: &mut ProgramState) {
    let path = std::env::var("SRTFIL").unwrap_or("srtfil.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.srtfil_handle = handle; state.srtfil_status = FileStatus::Success; }
        Err(_) => { state.srtfil_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ SRTFIL
fn srtfil_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.srtfil_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(256);
            if _len > 0 { let _end = _len.min(0+256); state.srtrec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.srtfil_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.srtfil_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.srtfil_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to SRTFIL
fn srtfil_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 256];
    { let _src = state.srtrec.as_bytes(); let _end = (0+256).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.srtfil_handle.write_record(&_buf) {
        Ok(()) => state.srtfil_status = FileStatus::Success,
        Err(e) => state.srtfil_status = e,
    }
}

/// CLOSE SRTFIL
fn srtfil_close(state: &mut ProgramState) {
    match state.srtfil_handle.close() {
        Ok(()) => state.srtfil_status = FileStatus::Success,
        Err(e) => state.srtfil_status = e,
    }
}

/// OPEN INPUT STFILE
fn stfile_open_input(state: &mut ProgramState) {
    let path = std::env::var("STFILE").unwrap_or("stfile.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state.stfile_handle = handle; state.stfile_status = FileStatus::Success; }
        Err(_) => { state.stfile_status = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT STFILE
fn stfile_open_output(state: &mut ProgramState) {
    let path = std::env::var("STFILE").unwrap_or("stfile.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state.stfile_handle = handle; state.stfile_status = FileStatus::Success; }
        Err(_) => { state.stfile_status = FileStatus::Error("35".to_string()); }
    }
}

/// READ STFILE
fn stfile_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state.stfile_handle.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(256);
            if _len > 0 { let _end = _len.min(0+256); state.stfrec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state.stfile_status = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state.stfile_status = FileStatus::AtEnd;
        }
        Err(e) => {
            state.stfile_status = e.clone();
        }
    }
    Ok(())
}

/// WRITE to STFILE
fn stfile_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 256];
    { let _src = state.stfrec.as_bytes(); let _end = (0+256).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state.stfile_handle.write_record(&_buf) {
        Ok(()) => state.stfile_status = FileStatus::Success,
        Err(e) => state.stfile_status = e,
    }
}

/// CLOSE STFILE
fn stfile_close(state: &mut ProgramState) {
    match state.stfile_handle.close() {
        Ok(()) => state.stfile_status = FileStatus::Success,
        Err(e) => state.stfile_status = e,
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_x(&mut state);
}

