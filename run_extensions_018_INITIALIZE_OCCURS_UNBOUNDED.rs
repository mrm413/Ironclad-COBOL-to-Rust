// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.


use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct ATable {
    /// PREFIX
    pub prefix: Prefix,
    /// TABLE-DATA
    pub table_data: TableData,
}
impl std::fmt::Display for ATable {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.prefix, self.table_data)
    }
}
impl From<&str> for ATable {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for ATable {
    fn from(_s: String) -> Self { Self::default() }
}
impl ATable {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Prefix {
    /// N
    pub n: u32,
}
impl std::fmt::Display for Prefix {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.n)
    }
}
impl From<&str> for Prefix {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Prefix {
    fn from(_s: String) -> Self { Self::default() }
}
impl Prefix {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct TableData {
    /// ROWS
    pub rows: [Rows; 0],
}
impl std::fmt::Display for TableData {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "")
    }
}
impl From<&str> for TableData {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for TableData {
    fn from(_s: String) -> Self { Self::default() }
}
impl TableData {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Rows {
    /// COL1
    pub col1: FixedString<1>,
    /// COL2
    pub col2: FixedString<2>,
}
impl std::fmt::Display for Rows {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.col1, self.col2)
    }
}
impl From<&str> for Rows {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Rows {
    fn from(_s: String) -> Self { Self::default() }
}
impl Rows {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// P
    pub p: FixedString<1>,
    /// P2
    pub p2: FixedString<1>,
    /// DLEN
    pub dlen: u32,
    /// LINKAGE: A-TABLE
    pub a_table: ATable,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            p: Default::default(),
            p2: Default::default(),
            dlen: Default::default(),
            a_table: Default::default(),
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// DLEN
fn p_dlen(state: &mut ProgramState) {
    // SKIP: unresolved ref
    if &format!("{}", state.a_table.table_data.rows[((1 - 1) as usize).min(0)].col2) != &"BC".to_string() {
        println!("{}{}", "col2(1) wrong: ", state.a_table.table_data.rows[((1 - 1) as usize).min(0)].col2);
        if { let _s = format!("{}", state.a_table.table_data.rows[((2 - 1) as usize).min(0)]); _s.trim().to_string() } != "DEA".to_string() {
            println!("{}{}", "rows(2) wrong: ", state.a_table.table_data.rows[((2 - 1) as usize).min(0)]);
            state.a_table.prefix.n = 2 as u32;
            // SKIP: unresolved ref
            state.a_table.prefix.n = 7 as u32;
            if { let _s = format!("{}", state.a_table.table_data.rows[((2 - 1) as usize).min(0)]); _s.trim().to_string() } != "000".to_string() {
                println!("{}{}", "rows(2) after ref-mod MOVE wrong: ", state.a_table.table_data.rows[((2 - 1) as usize).min(0)]);
                if { let _s = format!("{}", state.a_table.table_data.rows[((3 - 1) as usize).min(0)]); _s.trim().to_string() } != "BCD".to_string() {
                    println!("{}{}", "rows(3) after ref-mod MOVE wrong: ", state.a_table.table_data.rows[((3 - 1) as usize).min(0)]);
                    // SKIP: unresolved ref
                    state.a_table.prefix.n = 4 as u32;
                    state.a_table.table_data = Default::default();
                    state.a_table.prefix.n = 7 as u32;
                    if ({ let _s = format!("{}", state.a_table.table_data.rows[((2 - 1) as usize).min(0)]); _s.trim().to_string() } != " ".to_string()) || ({ let _s = format!("{}", state.a_table.table_data.rows[((4 - 1) as usize).min(0)]); _s.trim().to_string() } != " ".to_string()) {
                        println!("{}{}{}", "rows(2/4) after init default wrong: ", state.a_table.table_data.rows[((2 - 1) as usize).min(0)], state.a_table.table_data.rows[((4 - 1) as usize).min(0)]);
                        if { let _s = format!("{}", state.a_table.table_data.rows[((5 - 1) as usize).min(0)]); _s.trim().to_string() } != "999".to_string() {
                            println!("{}{}", "rows(5) after init default wrong: ", state.a_table.table_data.rows[((5 - 1) as usize).min(0)]);
                            state.a_table.prefix.n = 6 as u32;
                            state.a_table.table_data = Default::default();
                            state.a_table.prefix.n = 9 as u32;
                            if { let _s = format!("{}", state.a_table.table_data.rows[((5 - 1) as usize).min(0)]); _s.trim().to_string() } != "CDE".to_string() {
                                println!("{}{}", "rows(5) after init value wrong: ", state.a_table.table_data.rows[((5 - 1) as usize).min(0)]);
                                if { let _s = format!("{}", state.a_table.table_data.rows[((7 - 1) as usize).min(0)]); _s.trim().to_string() } != "999".to_string() {
                                    println!("{}{}", "rows(7) after init value wrong: ", state.a_table.table_data.rows[((7 - 1) as usize).min(0)]);
                                    if { let _s = format!("{}", { let _rm_s = state.a_table.to_string(); String::from(&_rm_s[(20 - 1) as usize..(20 - 1 + 10) as usize]) }); _s.trim().to_string() } != "BC999BCDEA".to_string() {
                                        println!("{}{}", "a-table / ref-mod wrong: ", { let _rm_s = state.a_table.to_string(); String::from(&_rm_s[(20 - 1) as usize..(20 - 1 + 10) as usize]) });
                                        state.a_table.prefix.n = 10 as u32;
                                        if { let _s = format!("{}", { let _rm_s = state.a_table.to_string(); String::from(&_rm_s[(20 - 1) as usize..(20 - 1 + state.a_table.prefix.n) as usize]) }); _s.trim().to_string() } != "BC999BCDEA".to_string() {
                                            println!("{}{}", "a-table / ref-mod length wrong: ", { let _rm_s = state.a_table.to_string(); String::from(&_rm_s[(20 - 1) as usize..(20 - 1 + state.a_table.prefix.n) as usize]) });
                                            state.a_table.prefix.n = 20 as u32;
                                            if { let _s = format!("{}", { let _rm_s = state.a_table.to_string(); String::from(&_rm_s[(state.a_table.prefix.n - 1) as usize..(state.a_table.prefix.n - 1 + 10) as usize]) }); _s.trim().to_string() } != "BC999BCDEA".to_string() {
                                                println!("{}{}", "a-table / ref-mod offset wrong: ", { let _rm_s = state.a_table.to_string(); String::from(&_rm_s[(state.a_table.prefix.n - 1) as usize..(state.a_table.prefix.n - 1 + 10) as usize]) });
                                                state.a_table.prefix.n = 8 as u32;
                                                state.a_table.table_data = Default::default();
                                                println!("{}", { let _rm_s = state.a_table.to_string(); String::from(&_rm_s[(20 - 1) as usize..(20 - 1 + 10) as usize]) });
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_dlen(&mut state);
}

