// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct StckRec {
    /// PEOPLE-LASTNAME
    pub people_lastname: FixedString<50>,
    /// PEOPLE-FIRSTNAME
    pub people_firstname: FixedString<50>,
}
impl std::fmt::Display for StckRec {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.people_lastname, self.people_firstname)
    }
}
impl From<&str> for StckRec {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for StckRec {
    fn from(_s: String) -> Self { Self::default() }
}
impl StckRec {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct StckRecPart {
    /// PEOPLE-1STNAME
    pub people_1stname: FixedString<50>,
}
impl std::fmt::Display for StckRecPart {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.people_1stname)
    }
}
impl From<&str> for StckRecPart {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for StckRecPart {
    fn from(_s: String) -> Self { Self::default() }
}
impl StckRecPart {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct StckFile {
    /// STCK-REC
    pub stck_rec: StckRec,
    /// STCK-REC-PART
    pub stck_rec_part: StckRecPart,
}
impl std::fmt::Display for StckFile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.stck_rec, self.stck_rec_part)
    }
}
impl From<&str> for StckFile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for StckFile {
    fn from(_s: String) -> Self { Self::default() }
}
impl StckFile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct LocalRec {
    /// LPEOPLE-LASTNAME
    pub lpeople_lastname: FixedString<50>,
    /// LPEOPLE-FIRSTNAME
    pub lpeople_firstname: FixedString<50>,
}
impl std::fmt::Display for LocalRec {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.lpeople_lastname, self.lpeople_firstname)
    }
}
impl From<&str> for LocalRec {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for LocalRec {
    fn from(_s: String) -> Self { Self::default() }
}
impl LocalRec {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct LocalFile {
    /// LOCAL-REC
    pub local_rec: LocalRec,
}
impl std::fmt::Display for LocalFile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.local_rec)
    }
}
impl From<&str> for LocalFile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for LocalFile {
    fn from(_s: String) -> Self { Self::default() }
}
impl LocalFile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct FileStates {
    /// STCK-FILE-STATUS
    pub stck_file_status: FixedString<2>,
    /// LOCL-FILE-STATUS
    pub locl_file_status: FixedString<2>,
}
impl std::fmt::Display for FileStates {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.stck_file_status, self.locl_file_status)
    }
}
impl From<&str> for FileStates {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for FileStates {
    fn from(_s: String) -> Self { Self::default() }
}
impl FileStates {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD group: STCK-REC
    pub stck_rec: FixedString<100>,
    /// FD: PEOPLE-LASTNAME
    pub people_lastname: FixedString<50>,
    /// FD: PEOPLE-FIRSTNAME
    pub people_firstname: FixedString<50>,
    /// FD group: STCK-REC-PART
    pub stck_rec_part: FixedString<50>,
    /// FD: PEOPLE-1STNAME
    pub people_1stname: FixedString<50>,
    /// FD group: LOCAL-REC
    pub local_rec: FixedString<100>,
    /// FD: LPEOPLE-LASTNAME
    pub lpeople_lastname: FixedString<50>,
    /// FD: LPEOPLE-FIRSTNAME
    pub lpeople_firstname: FixedString<50>,
    /// FILE-STATES
    pub file_states: FileStates,
    /// STCK-FILE-STATUS
    pub stck_file_status: FixedString<2>,
    /// LOCL-FILE-STATUS
    pub locl_file_status: FixedString<2>,
    /// File status for STCK-FILE
    pub _fs_stck_file: FileStatus,
    /// File handle for STCK-FILE
    pub _fh_stck_file: CobolFile,
    /// File status for LOCAL-FILE
    pub _fs_local_file: FileStatus,
    /// File handle for LOCAL-FILE
    pub _fh_local_file: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            stck_rec: Default::default(),
            people_lastname: Default::default(),
            people_firstname: Default::default(),
            stck_rec_part: Default::default(),
            people_1stname: Default::default(),
            local_rec: Default::default(),
            lpeople_lastname: Default::default(),
            lpeople_firstname: Default::default(),
            file_states: Default::default(),
            stck_file_status: Default::default(),
            locl_file_status: Default::default(),
            _fs_stck_file: FileStatus::Success,
            _fh_stck_file: CobolFile::Closed,
            _fs_local_file: FileStatus::Success,
            _fh_local_file: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// SECTION
fn p_section(state: &mut ProgramState) {
    local_file_open_output(state);
    state.lpeople_firstname = "John".into();
    state.lpeople_lastname = "Doe".into();
    local_rec_write(state);
    local_file_close(state);
    stck_file_open_output(state);
    state.people_firstname = "John".into();
    state.people_lastname = "Doe".into();
    stck_rec_write(state);
    stck_file_close(state);
    inner_program();
    { let _ = state._fh_stck_file.close();
        let _ = state._fh_local_file.close();
        return; }
}

/// PROGRAM-ID
fn p_program_id(state: &mut ProgramState) {
}

/// INNER-PROGRAM
fn p_inner_program(state: &mut ProgramState) {
}

/// DIVISION
fn p_division(state: &mut ProgramState) {
    stck_file_open_input(state);
    state.people_firstname = "John".into();
    state.people_lastname = "Doe".into();
    stck_file_read(state);
    // SKIP: unresolved display ref
    println!("{}", "NOT FOUND");
    state.stck_rec = Default::default();
    state.people_1stname = "John".into();
    stck_file_read(state);
    // SKIP: unresolved display ref
    println!("{}", "NOT FOUND");
    stck_file_close(state);
    local_file_open_input(state);
    state.lpeople_firstname = "John".into();
    state.lpeople_lastname = "Doe".into();
    local_file_read(state);
    // SKIP: unresolved display ref
    println!("{}", "NOT FOUND");
    local_file_close(state);
    { let _ = state._fh_stck_file.close();
        let _ = state._fh_local_file.close();
        return; }
}

/// INNER-PROGRAM
fn p_inner_program_2(state: &mut ProgramState) {
}

/// OUTER-PROGRAM
fn p_outer_program(state: &mut ProgramState) {
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT STCK-FILE
fn stck_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN OUTPUT STCK-FILE
fn stck_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// READ STCK-FILE
fn stck_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_stck_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+50); state.people_lastname = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 50 { let _end = _len.min(50+50); state.people_firstname = FixedString::from(String::from_utf8_lossy(&_bytes[50.._end])); }
            if _len > 100 { let _end = _len.min(100+50); state.people_1stname = FixedString::from(String::from_utf8_lossy(&_bytes[100.._end])); }
            state._fs_stck_file = FileStatus::Success;
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
        }
        Err(FileStatus::AtEnd) => {
            state._fs_stck_file = FileStatus::AtEnd;
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
        }
        Err(e) => {
            state._fs_stck_file = e.clone();
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
        }
    }
    Ok(())
}

/// WRITE to STCK-FILE
fn stck_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.people_lastname.as_bytes(); let _end = (0+50).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.people_firstname.as_bytes(); let _end = (50+50).min(_buf.len()); let _cl = _src.len().min(_end-50); _buf[50..50+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.people_1stname.as_bytes(); let _end = (100+50).min(_buf.len()); let _cl = _src.len().min(_end-100); _buf[100..100+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_stck_file.write_record(&_buf) {
        Ok(()) => state._fs_stck_file = FileStatus::Success,
        Err(e) => state._fs_stck_file = e,
    }
}

/// CLOSE STCK-FILE
fn stck_file_close(state: &mut ProgramState) {
    match state._fh_stck_file.close() {
        Ok(()) => state._fs_stck_file = FileStatus::Success,
        Err(e) => state._fs_stck_file = e,
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN I-O STCK-FILE
fn stck_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN EXTEND STCK-FILE
fn stck_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN INPUT LOCAL-FILE
fn local_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
}

/// OPEN OUTPUT LOCAL-FILE
fn local_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
}

/// READ LOCAL-FILE
fn local_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_local_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+50); state.lpeople_lastname = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 50 { let _end = _len.min(50+50); state.lpeople_firstname = FixedString::from(String::from_utf8_lossy(&_bytes[50.._end])); }
            state._fs_local_file = FileStatus::Success;
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
        }
        Err(FileStatus::AtEnd) => {
            state._fs_local_file = FileStatus::AtEnd;
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
        }
        Err(e) => {
            state._fs_local_file = e.clone();
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
        }
    }
    Ok(())
}

/// WRITE to LOCAL-FILE
fn local_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.lpeople_lastname.as_bytes(); let _end = (0+50).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.lpeople_firstname.as_bytes(); let _end = (50+50).min(_buf.len()); let _cl = _src.len().min(_end-50); _buf[50..50+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_local_file.write_record(&_buf) {
        Ok(()) => state._fs_local_file = FileStatus::Success,
        Err(e) => state._fs_local_file = e,
    }
}

/// CLOSE LOCAL-FILE
fn local_file_close(state: &mut ProgramState) {
    match state._fh_local_file.close() {
        Ok(()) => state._fs_local_file = FileStatus::Success,
        Err(e) => state._fs_local_file = e,
    }
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
}

/// OPEN I-O LOCAL-FILE
fn local_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
}

/// OPEN EXTEND LOCAL-FILE
fn local_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = FixedString::from(format!("{}", state._fs_local_file));
}

/// WRITE LOCAL-REC
fn local_rec_write(state: &mut ProgramState) {
    // Write LOCAL-REC via local_file_write (byte buffer)
    local_file_write(state);
}

/// WRITE STCK-REC
fn stck_rec_write(state: &mut ProgramState) {
    // Write STCK-REC via stck_file_write (byte buffer)
    stck_file_write(state);
}

/// External: CALL 'INNER-PROGRAM'
fn inner_program() {
    todo!("External subroutine 'INNER-PROGRAM' not yet linked — implement or link the INNER-PROGRAM module")
}

fn main() {
    let mut state = ProgramState::default();
    p_section(&mut state);
}

