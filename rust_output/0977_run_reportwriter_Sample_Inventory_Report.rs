// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Source: PROG.cbl
// Do not edit manually. Regenerate from COBOL source.
#![allow(unused_imports, unused_variables, dead_code, unused_parens, non_snake_case, invalid_value)]

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
use cobol_runtime::string_ops::*;
use cobol_runtime::chrono_shim::*;
use cobol_runtime::CobolInto;
use cobol_runtime::cobol_helpers::*;
use cobol_runtime::define_record;

define_record! {
    /// INV-REC
    pub struct InvRec {
        /// DEPT-IN
        pub dept_in: Decimal,
        /// DEPT-NAM-IN
        pub dept_nam_in: FixedString<18>,
        /// MONTH-IN
        pub month_in: Decimal,
        /// ITEM-NO-IN
        pub item_no_in: Decimal,
        /// INV-TOT-IN
        pub inv_tot_in: Decimal,
    }
}

define_record! {
    /// INDICATORS
    pub struct Indicators {
        /// ARE-THERE-MORE-RECORDS
        pub are_there_more_records: FixedString<3>,
    }
}


pub struct ProgramState {
    // --- FD: INV-FILE ---
    /// FD: INV-REC (group)
    pub inv_rec: FixedString<35>,
    /// FD: DEPT-IN
    pub dept_in: Decimal,
    /// FD: DEPT-NAM-IN
    pub dept_nam_in: FixedString<18>,
    /// FD: MONTH-IN
    pub month_in: Decimal,
    /// FD: ITEM-NO-IN
    pub item_no_in: Decimal,
    /// FD: INV-TOT-IN
    pub inv_tot_in: Decimal,
    // --- FD: REPORT-FILE ---
    // --- WORKING-STORAGE SECTION ---
    /// WS: INDICATORS (group)
    pub indicators: FixedString<3>,
    /// WS: ARE-THERE-MORE-RECORDS
    pub are_there_more_records: FixedString<3>,
    // --- File handles ---
    /// File status for INV-FILE
    pub _fs_inv_file: FileStatus,
    /// File handle for INV-FILE
    pub _fh_inv_file: CobolFile,
    /// File status for REPORT-FILE
    pub _fs_report_file: FileStatus,
    /// File handle for REPORT-FILE
    pub _fh_report_file: CobolFile,
    // --- Special registers ---
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        unsafe { std::mem::MaybeUninit::zeroed().assume_init() }
    }
}

/// OPEN INPUT INV-FILE
fn inv_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("INV_FILE").unwrap_or("inv_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_inv_file = handle; state._fs_inv_file = FileStatus::Success; }
        Err(_) => { state._fs_inv_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT INV-FILE
fn inv_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("INV_FILE").unwrap_or("inv_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_inv_file = handle; state._fs_inv_file = FileStatus::Success; }
        Err(_) => { state._fs_inv_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN I-O INV-FILE
fn inv_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("INV_FILE").unwrap_or("inv_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_inv_file = handle; state._fs_inv_file = FileStatus::Success; }
        Err(_) => { state._fs_inv_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND INV-FILE
fn inv_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("INV_FILE").unwrap_or("inv_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_inv_file = handle; state._fs_inv_file = FileStatus::Success; }
        Err(_) => { state._fs_inv_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ INV-FILE
fn inv_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_inv_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(35);
            if _len > 0 { let _end = _len.min(0+2); let _s = String::from_utf8_lossy(&_bytes[0.._end]).trim().to_string(); state.dept_in = _s.cobol_into(); }
            if _len > 2 { let _end = _len.min(2+18); state.dept_nam_in = String::from_utf8_lossy(&_bytes[2.._end]).to_string().cobol_into(); }
            if _len > 20 { let _end = _len.min(20+2); let _s = String::from_utf8_lossy(&_bytes[20.._end]).trim().to_string(); state.month_in = _s.cobol_into(); }
            if _len > 22 { let _end = _len.min(22+5); let _s = String::from_utf8_lossy(&_bytes[22.._end]).trim().to_string(); state.item_no_in = _s.cobol_into(); }
            if _len > 27 { let _end = _len.min(27+8); let _s = String::from_utf8_lossy(&_bytes[27.._end]).trim().to_string(); state.inv_tot_in = _s.cobol_into(); }
            state._fs_inv_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_inv_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_inv_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to INV-FILE
fn inv_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 35];
    { let _s = format!("{:>width$}", state.dept_in, width = 2); let _b = _s.as_bytes(); let _cl = _b.len().min(2); _buf[0..0+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _src = state.dept_nam_in.as_bytes(); let _end = (2+18).min(_buf.len()); let _cl = _src.len().min(_end-2); _buf[2..2+_cl].copy_from_slice(&_src[.._cl]); }
    { let _s = format!("{:>width$}", state.month_in, width = 2); let _b = _s.as_bytes(); let _cl = _b.len().min(2); _buf[20..20+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.item_no_in, width = 5); let _b = _s.as_bytes(); let _cl = _b.len().min(5); _buf[22..22+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.inv_tot_in, width = 8); let _b = _s.as_bytes(); let _cl = _b.len().min(8); _buf[27..27+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    match state._fh_inv_file.write_record(&_buf) {
        Ok(()) => state._fs_inv_file = FileStatus::Success,
        Err(e) => state._fs_inv_file = e,
    }
}

/// CLOSE INV-FILE
fn inv_file_close(state: &mut ProgramState) {
    match state._fh_inv_file.close() {
        Ok(()) => state._fs_inv_file = FileStatus::Success,
        Err(e) => state._fs_inv_file = e,
    }
}

/// OPEN INPUT REPORT-FILE
fn report_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("REPORT_FILE").unwrap_or("report_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_report_file = handle; state._fs_report_file = FileStatus::Success; }
        Err(_) => { state._fs_report_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT REPORT-FILE
fn report_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("REPORT_FILE").unwrap_or("report_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_report_file = handle; state._fs_report_file = FileStatus::Success; }
        Err(_) => { state._fs_report_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN I-O REPORT-FILE
fn report_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("REPORT_FILE").unwrap_or("report_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_report_file = handle; state._fs_report_file = FileStatus::Success; }
        Err(_) => { state._fs_report_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND REPORT-FILE
fn report_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("REPORT_FILE").unwrap_or("report_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_report_file = handle; state._fs_report_file = FileStatus::Success; }
        Err(_) => { state._fs_report_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ REPORT-FILE
fn report_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_report_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(0);
            state._fs_report_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_report_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_report_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to REPORT-FILE
fn report_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 0];
    match state._fh_report_file.write_record(&_buf) {
        Ok(()) => state._fs_report_file = FileStatus::Success,
        Err(e) => state._fs_report_file = e,
    }
}

/// CLOSE REPORT-FILE
fn report_file_close(state: &mut ProgramState) {
    match state._fh_report_file.close() {
        Ok(()) => state._fs_report_file = FileStatus::Success,
        Err(e) => state._fs_report_file = e,
    }
}

/// Paragraph: A000-MAINLINE
fn p_a000_mainline(state: &mut ProgramState) {
    inv_file_open_input(state);
    // OUTPUT REPORT-FILE
    // INITIATE INV-REPORT
    let _ = inv_file_read(state);
    if state._fs_inv_file == FileStatus::AtEnd {
        state.are_there_more_records = format!("{}", "NO ").cobol_into();
    }
    while !((format!("{}", state.are_there_more_records).trim() == format!("{}", "NO ").trim())) {
        p_a001_loop(state);
    }
    // TERMINATE INV-REPORT
    inv_file_close(state);
    report_file_close(state);
    std::process::exit(0);
}

/// Paragraph: A001-LOOP
fn p_a001_loop(state: &mut ProgramState) {
    // GENERATE INV-DETAIL
    let _ = inv_file_read(state);
    if state._fs_inv_file == FileStatus::AtEnd {
        state.are_there_more_records = format!("{}", "NO ").cobol_into();
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_a000_mainline(&mut state);
}
