// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct F {
    /// F-REC
    pub f_rec: FixedString<10>,
}
impl std::fmt::Display for F {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.f_rec)
    }
}
impl From<&str> for F {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for F {
    fn from(_s: String) -> Self { Self::default() }
}
impl F {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct G {
    /// G-REC
    pub g_rec: FixedString<10>,
}
impl std::fmt::Display for G {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.g_rec)
    }
}
impl From<&str> for G {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for G {
    fn from(_s: String) -> Self { Self::default() }
}
impl G {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct H {
    /// H-REC
    pub h_rec: FixedString<10>,
}
impl std::fmt::Display for H {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.h_rec)
    }
}
impl From<&str> for H {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for H {
    fn from(_s: String) -> Self { Self::default() }
}
impl H {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct I {
    /// I-REC
    pub i_rec: FixedString<10>,
}
impl std::fmt::Display for I {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.i_rec)
    }
}
impl From<&str> for I {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for I {
    fn from(_s: String) -> Self { Self::default() }
}
impl I {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: F-REC
    pub f_rec: FixedString<10>,
    /// FD: G-REC
    pub g_rec: FixedString<10>,
    /// FD: H-REC
    pub h_rec: FixedString<10>,
    /// FD: I-REC
    pub i_rec: FixedString<10>,
    /// File status for F
    pub _fs_f: FileStatus,
    /// File handle for F
    pub _fh_f: CobolFile,
    /// File status for G
    pub _fs_g: FileStatus,
    /// File handle for G
    pub _fh_g: CobolFile,
    /// File status for H
    pub _fs_h: FileStatus,
    /// File handle for H
    pub _fh_h: CobolFile,
    /// File status for I
    pub _fs_i: FileStatus,
    /// File handle for I
    pub _fh_i: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            f_rec: Default::default(),
            g_rec: Default::default(),
            h_rec: Default::default(),
            i_rec: Default::default(),
            _fs_f: FileStatus::Success,
            _fh_f: CobolFile::Closed,
            _fs_g: FileStatus::Success,
            _fh_g: CobolFile::Closed,
            _fs_h: FileStatus::Success,
            _fh_h: CobolFile::Closed,
            _fs_i: FileStatus::Success,
            _fh_i: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT F
fn f_open_input(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT F
fn f_open_output(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
}

/// READ F
fn f_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_f.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(10);
            if _len > 0 { let _end = _len.min(0+10); state.f_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_f = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_f = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_f = e.clone();
        }
    }
    Ok(())
}

/// WRITE to F
fn f_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 10];
    { let _src = state.f_rec.as_bytes(); let _end = (0+10).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_f.write_record(&_buf) {
        Ok(()) => state._fs_f = FileStatus::Success,
        Err(e) => state._fs_f = e,
    }
}

/// CLOSE F
fn f_close(state: &mut ProgramState) {
    match state._fh_f.close() {
        Ok(()) => state._fs_f = FileStatus::Success,
        Err(e) => state._fs_f = e,
    }
}

/// OPEN I-O F
fn f_open_io(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND F
fn f_open_extend(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT G
fn g_open_input(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT G
fn g_open_output(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
}

/// READ G
fn g_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_g.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(10);
            if _len > 0 { let _end = _len.min(0+10); state.g_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_g = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_g = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_g = e.clone();
        }
    }
    Ok(())
}

/// WRITE to G
fn g_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 10];
    { let _src = state.g_rec.as_bytes(); let _end = (0+10).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_g.write_record(&_buf) {
        Ok(()) => state._fs_g = FileStatus::Success,
        Err(e) => state._fs_g = e,
    }
}

/// CLOSE G
fn g_close(state: &mut ProgramState) {
    match state._fh_g.close() {
        Ok(()) => state._fs_g = FileStatus::Success,
        Err(e) => state._fs_g = e,
    }
}

/// OPEN I-O G
fn g_open_io(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND G
fn g_open_extend(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT H
fn h_open_input(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT H
fn h_open_output(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
}

/// READ H
fn h_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_h.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(10);
            if _len > 0 { let _end = _len.min(0+10); state.h_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_h = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_h = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_h = e.clone();
        }
    }
    Ok(())
}

/// WRITE to H
fn h_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 10];
    { let _src = state.h_rec.as_bytes(); let _end = (0+10).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_h.write_record(&_buf) {
        Ok(()) => state._fs_h = FileStatus::Success,
        Err(e) => state._fs_h = e,
    }
}

/// CLOSE H
fn h_close(state: &mut ProgramState) {
    match state._fh_h.close() {
        Ok(()) => state._fs_h = FileStatus::Success,
        Err(e) => state._fs_h = e,
    }
}

/// OPEN I-O H
fn h_open_io(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND H
fn h_open_extend(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT I
fn i_open_input(state: &mut ProgramState) {
    let path = std::env::var("I").unwrap_or("i.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_i = handle; state._fs_i = FileStatus::Success; }
        Err(_) => { state._fs_i = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT I
fn i_open_output(state: &mut ProgramState) {
    let path = std::env::var("I").unwrap_or("i.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_i = handle; state._fs_i = FileStatus::Success; }
        Err(_) => { state._fs_i = FileStatus::Error("35".to_string()); }
    }
}

/// READ I
fn i_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_i.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(10);
            if _len > 0 { let _end = _len.min(0+10); state.i_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_i = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_i = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_i = e.clone();
        }
    }
    Ok(())
}

/// WRITE to I
fn i_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 10];
    { let _src = state.i_rec.as_bytes(); let _end = (0+10).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_i.write_record(&_buf) {
        Ok(()) => state._fs_i = FileStatus::Success,
        Err(e) => state._fs_i = e,
    }
}

/// CLOSE I
fn i_close(state: &mut ProgramState) {
    match state._fh_i.close() {
        Ok(()) => state._fs_i = FileStatus::Success,
        Err(e) => state._fs_i = e,
    }
}

/// OPEN I-O I
fn i_open_io(state: &mut ProgramState) {
    let path = std::env::var("I").unwrap_or("i.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_i = handle; state._fs_i = FileStatus::Success; }
        Err(_) => { state._fs_i = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND I
fn i_open_extend(state: &mut ProgramState) {
    let path = std::env::var("I").unwrap_or("i.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_i = handle; state._fs_i = FileStatus::Success; }
        Err(_) => { state._fs_i = FileStatus::Error("35".to_string()); }
    }
}

fn main() {
    // No paragraphs found
}

