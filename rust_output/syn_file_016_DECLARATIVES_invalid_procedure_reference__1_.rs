// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct GoFile {
    /// GO-REC
    pub go_rec: FixedString<4>,
}
impl std::fmt::Display for GoFile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.go_rec)
    }
}
impl From<&str> for GoFile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for GoFile {
    fn from(_s: String) -> Self { Self::default() }
}
impl GoFile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct PerfFile {
    /// PERF-REC
    pub perf_rec: FixedString<4>,
}
impl std::fmt::Display for PerfFile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.perf_rec)
    }
}
impl From<&str> for PerfFile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for PerfFile {
    fn from(_s: String) -> Self { Self::default() }
}
impl PerfFile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: GO-REC
    pub go_rec: FixedString<4>,
    /// FD: PERF-REC
    pub perf_rec: FixedString<4>,
    /// File status for GO-FILE
    pub _fs_go_file: FileStatus,
    /// File handle for GO-FILE
    pub _fh_go_file: CobolFile,
    /// File status for PERF-FILE
    pub _fs_perf_file: FileStatus,
    /// File handle for PERF-FILE
    pub _fh_perf_file: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            go_rec: Default::default(),
            perf_rec: Default::default(),
            _fs_go_file: FileStatus::Success,
            _fh_go_file: CobolFile::Closed,
            _fs_perf_file: FileStatus::Success,
            _fh_perf_file: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// DECLARATIVES
fn p_declaratives(state: &mut ProgramState) {
}

/// SECTION
fn p_section(state: &mut ProgramState) {
}

/// GO-FILE
fn p_go_file(state: &mut ProgramState) {
}

/// G02
fn p_g02(state: &mut ProgramState) {
    println!("{}", "OK");
    p_gg02(state); return;
}

/// SECTION
fn p_section_2(state: &mut ProgramState) {
}

/// PERF-FILE
fn p_perf_file(state: &mut ProgramState) {
}

/// P02
fn p_p02(state: &mut ProgramState) {
    println!("{}", "OK");
    p_ppok(state);
    p_pp02(state);
}

/// DECLARATIVES
fn p_declaratives_2(state: &mut ProgramState) {
}

/// SECTION
fn p_section_3(state: &mut ProgramState) {
}

/// GG02
fn p_gg02(state: &mut ProgramState) {
    go_file_open_input(state);
    go_file_close(state);
}

/// SECTION
fn p_section_4(state: &mut ProgramState) {
}

/// PP02
fn p_pp02(state: &mut ProgramState) {
    perf_file_open_input(state);
    perf_file_close(state);
}

/// PP03
fn p_pp03(state: &mut ProgramState) {
    println!("{}", "LOG OUTPUT HERE");
}

/// PPOK
fn p_ppok(state: &mut ProgramState) {
    { let _ = state._fh_go_file.close();
        let _ = state._fh_perf_file.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT GO-FILE
fn go_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("GO_FILE").unwrap_or("go_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_go_file = handle; state._fs_go_file = FileStatus::Success; }
        Err(_) => { state._fs_go_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT GO-FILE
fn go_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("GO_FILE").unwrap_or("go_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_go_file = handle; state._fs_go_file = FileStatus::Success; }
        Err(_) => { state._fs_go_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ GO-FILE
fn go_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_go_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(4);
            if _len > 0 { let _end = _len.min(0+4); state.go_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_go_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_go_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_go_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to GO-FILE
fn go_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 4];
    { let _src = state.go_rec.as_bytes(); let _end = (0+4).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_go_file.write_record(&_buf) {
        Ok(()) => state._fs_go_file = FileStatus::Success,
        Err(e) => state._fs_go_file = e,
    }
}

/// CLOSE GO-FILE
fn go_file_close(state: &mut ProgramState) {
    match state._fh_go_file.close() {
        Ok(()) => state._fs_go_file = FileStatus::Success,
        Err(e) => state._fs_go_file = e,
    }
}

/// OPEN I-O GO-FILE
fn go_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("GO_FILE").unwrap_or("go_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_go_file = handle; state._fs_go_file = FileStatus::Success; }
        Err(_) => { state._fs_go_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND GO-FILE
fn go_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("GO_FILE").unwrap_or("go_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_go_file = handle; state._fs_go_file = FileStatus::Success; }
        Err(_) => { state._fs_go_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT PERF-FILE
fn perf_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("PERF_FILE").unwrap_or("perf_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_perf_file = handle; state._fs_perf_file = FileStatus::Success; }
        Err(_) => { state._fs_perf_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT PERF-FILE
fn perf_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("PERF_FILE").unwrap_or("perf_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_perf_file = handle; state._fs_perf_file = FileStatus::Success; }
        Err(_) => { state._fs_perf_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ PERF-FILE
fn perf_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_perf_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(4);
            if _len > 0 { let _end = _len.min(0+4); state.perf_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_perf_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_perf_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_perf_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to PERF-FILE
fn perf_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 4];
    { let _src = state.perf_rec.as_bytes(); let _end = (0+4).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_perf_file.write_record(&_buf) {
        Ok(()) => state._fs_perf_file = FileStatus::Success,
        Err(e) => state._fs_perf_file = e,
    }
}

/// CLOSE PERF-FILE
fn perf_file_close(state: &mut ProgramState) {
    match state._fh_perf_file.close() {
        Ok(()) => state._fs_perf_file = FileStatus::Success,
        Err(e) => state._fs_perf_file = e,
    }
}

/// OPEN I-O PERF-FILE
fn perf_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("PERF_FILE").unwrap_or("perf_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_perf_file = handle; state._fs_perf_file = FileStatus::Success; }
        Err(_) => { state._fs_perf_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND PERF-FILE
fn perf_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("PERF_FILE").unwrap_or("perf_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_perf_file = handle; state._fs_perf_file = FileStatus::Success; }
        Err(_) => { state._fs_perf_file = FileStatus::Error("35".to_string()); }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_declaratives(&mut state);
}

