// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct Input {
    /// INPUT-REC
    pub input_rec: FixedString<1>,
}
impl std::fmt::Display for Input {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.input_rec)
    }
}
impl From<&str> for Input {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Input {
    fn from(_s: String) -> Self { Self::default() }
}
impl Input {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: INPUT-REC
    pub input_rec: FixedString<1>,
    /// File status for INPUT
    pub _fs_input: FileStatus,
    /// File handle for INPUT
    pub _fh_input: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            input_rec: Default::default(),
            _fs_input: FileStatus::Success,
            _fh_input: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT INPUT
fn input_open_input(state: &mut ProgramState) {
    let path = std::env::var("INPUT").unwrap_or("input.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_input = handle; state._fs_input = FileStatus::Success; }
        Err(_) => { state._fs_input = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT INPUT
fn input_open_output(state: &mut ProgramState) {
    let path = std::env::var("INPUT").unwrap_or("input.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_input = handle; state._fs_input = FileStatus::Success; }
        Err(_) => { state._fs_input = FileStatus::Error("35".to_string()); }
    }
}

/// READ INPUT
fn input_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_input.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.input_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_input = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_input = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_input = e.clone();
        }
    }
    Ok(())
}

/// WRITE to INPUT
fn input_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.input_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_input.write_record(&_buf) {
        Ok(()) => state._fs_input = FileStatus::Success,
        Err(e) => state._fs_input = e,
    }
}

/// CLOSE INPUT
fn input_close(state: &mut ProgramState) {
    match state._fh_input.close() {
        Ok(()) => state._fs_input = FileStatus::Success,
        Err(e) => state._fs_input = e,
    }
}

/// OPEN I-O INPUT
fn input_open_io(state: &mut ProgramState) {
    let path = std::env::var("INPUT").unwrap_or("input.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_input = handle; state._fs_input = FileStatus::Success; }
        Err(_) => { state._fs_input = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND INPUT
fn input_open_extend(state: &mut ProgramState) {
    let path = std::env::var("INPUT").unwrap_or("input.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_input = handle; state._fs_input = FileStatus::Success; }
        Err(_) => { state._fs_input = FileStatus::Error("35".to_string()); }
    }
}

fn main() {
    // No paragraphs found
}

