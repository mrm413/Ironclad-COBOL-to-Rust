// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.


use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone)]
pub struct ProgramState {
    /// TRGT-STRING
    pub trgt_string: FixedString<3>,
    /// STR-POINTER
    pub str_pointer: u32,
    /// SRC-DELIM
    pub src_delim: FixedString<1>,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            trgt_string: FixedString::new(),
            str_pointer: Default::default(),
            src_delim: Default::default(),
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// STR-POINTER
fn p_str_pointer(state: &mut ProgramState) {
    {
        let mut _str_ptr = state.str_pointer as usize;
        let _src = string_delimited("A".as_bytes(), b"B");
        string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, _src);
        string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, "C".as_bytes());
        state.str_pointer = _str_ptr as u32;
    }
    if &format!("{}", state.trgt_string) != &"ABC".to_string() {
        println!("{}{}{}", "A: TRTG-STRING <", state.trgt_string, "> != <ABC>");
        if state.str_pointer != 4 {
            println!("{}{}{}", "A: STR-POINTER <", state.str_pointer, "> != <04>");
            state.str_pointer = 0 as u32;
            state.trgt_string = FixedString::spaces();
            {
                let mut _str_ptr = state.str_pointer as usize;
                let _src = string_delimited("A".as_bytes(), b"B");
                string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, _src);
                string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, "C".as_bytes());
                state.str_pointer = _str_ptr as u32;
            }
            if state.trgt_string.trimmed() != " " {
                println!("{}{}{}", "B: TRTG-STRING <", state.trgt_string, "> != SPACES");
                if state.str_pointer != 0 {
                    println!("{}{}{}", "B: STR-POINTER <", state.str_pointer, "> != <00>");
                    state.str_pointer = 4 as u32;
                    state.trgt_string = FixedString::spaces();
                    {
                        let mut _str_ptr = state.str_pointer as usize;
                        let _src = string_delimited("A".as_bytes(), b"B");
                        string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, _src);
                        string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, "C".as_bytes());
                        state.str_pointer = _str_ptr as u32;
                    }
                    if state.trgt_string.trimmed() != " " {
                        println!("{}{}{}", "C: TRTG-STRING <", state.trgt_string, "> != SPACES");
                        if state.str_pointer != 4 {
                            println!("{}{}{}", "C: STR-POINTER <", state.str_pointer, "> != <04>");
                            state.str_pointer = 1 as u32;
                            state.src_delim = "|".into();
                            state.trgt_string = FixedString::spaces();
                            {
                                let mut _str_ptr = state.str_pointer as usize;
                                let _src = string_delimited("1|2".as_bytes(), b"A|B");
                                string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, _src);
                                let _src = string_delimited("C|D".as_bytes(), state.src_delim.as_bytes());
                                string_append(state.trgt_string.as_bytes_mut(), &mut _str_ptr, _src);
                                state.str_pointer = _str_ptr as u32;
                            }
                            if &format!("{}", state.trgt_string) != &"1AC".to_string() {
                                println!("{}{}{}", "D: TRGT-STRING <", state.trgt_string, "> != <1AC>");
                                return;
                            }
                        }
                    }
                }
            }
        }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_str_pointer(&mut state);
}

