// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Source: OUTER-PROGRAM.cbl
// Do not edit manually. Regenerate from COBOL source.
#![allow(unused_imports, unused_variables, dead_code, unused_parens, non_snake_case)]

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
use cobol_runtime::string_ops::*;
use cobol_runtime::chrono_shim::*;
use cobol_runtime::CobolInto;
use cobol_runtime::cobol_helpers::*;
use cobol_runtime::define_record;

define_record! {
    /// STCK-REC
    pub struct StckRec {
        /// PEOPLE-LASTNAME
        pub people_lastname: FixedString<50>,
        /// PEOPLE-FIRSTNAME
        pub people_firstname: FixedString<50>,
    }
}

define_record! {
    /// STCK-REC-PART
    pub struct StckRecPart {
        /// PEOPLE-1STNAME
        pub people_1stname: FixedString<50>,
    }
}

define_record! {
    /// LOCAL-REC
    pub struct LocalRec {
        /// LPEOPLE-LASTNAME
        pub lpeople_lastname: FixedString<50>,
        /// LPEOPLE-FIRSTNAME
        pub lpeople_firstname: FixedString<50>,
    }
}

define_record! {
    /// FILE-STATES
    pub struct FileStates {
        /// STCK-FILE-STATUS
        pub stck_file_status: FixedString<2>,
        /// LOCL-FILE-STATUS
        pub locl_file_status: FixedString<2>,
    }
}


#[derive(Default)]
pub struct ProgramState {
    // --- FD: STCK-FILE ---
    /// FD: STCK-REC (group)
    pub stck_rec: FixedString<100>,
    /// FD: PEOPLE-LASTNAME
    pub people_lastname: FixedString<50>,
    /// FD: PEOPLE-FIRSTNAME
    pub people_firstname: FixedString<50>,
    /// FD: STCK-REC-PART (group)
    pub stck_rec_part: FixedString<50>,
    /// FD: PEOPLE-1STNAME
    pub people_1stname: FixedString<50>,
    // --- FD: LOCAL-FILE ---
    /// FD: LOCAL-REC (group)
    pub local_rec: FixedString<100>,
    /// FD: LPEOPLE-LASTNAME
    pub lpeople_lastname: FixedString<50>,
    /// FD: LPEOPLE-FIRSTNAME
    pub lpeople_firstname: FixedString<50>,
    // --- WORKING-STORAGE SECTION ---
    /// WS: FILE-STATES (group)
    pub file_states: FixedString<4>,
    /// WS: STCK-FILE-STATUS
    pub stck_file_status: FixedString<2>,
    /// WS: LOCL-FILE-STATUS
    pub locl_file_status: FixedString<2>,
    // --- File handles ---
    /// File status for STCK-FILE
    pub _fs_stck_file: FileStatus,
    /// File handle for STCK-FILE
    pub _fh_stck_file: CobolFile,
    /// File status for LOCAL-FILE
    pub _fs_local_file: FileStatus,
    /// File handle for LOCAL-FILE
    pub _fh_local_file: CobolFile,
    // --- Special registers ---
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}


impl ProgramState {
    pub fn stck_file_ok(&self) -> bool { format!("{}", self.stck_file_status).trim() == "00" }
    pub fn locl_file_ok(&self) -> bool { format!("{}", self.locl_file_status).trim() == "00" }
}

/// OPEN INPUT STCK-FILE
fn stck_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
}

/// OPEN OUTPUT STCK-FILE
fn stck_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
}

/// OPEN I-O STCK-FILE
fn stck_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
}

/// OPEN EXTEND STCK-FILE
fn stck_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
}

/// READ STCK-FILE
fn stck_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_stck_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(150);
            if _len > 0 { let _end = _len.min(0+50); state.people_lastname = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            if _len > 50 { let _end = _len.min(50+50); state.people_firstname = String::from_utf8_lossy(&_bytes[50.._end]).to_string().cobol_into(); }
            if _len > 100 { let _end = _len.min(100+50); state.people_1stname = String::from_utf8_lossy(&_bytes[100.._end]).to_string().cobol_into(); }
            state._fs_stck_file = FileStatus::Success;
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
        }
        Err(FileStatus::AtEnd) => {
            state._fs_stck_file = FileStatus::AtEnd;
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
        }
        Err(e) => {
            state._fs_stck_file = e.clone();
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
        }
    }
    Ok(())
}

/// WRITE to STCK-FILE
fn stck_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 150];
    { let _src = state.people_lastname.as_bytes(); let _end = (0+50).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.people_firstname.as_bytes(); let _end = (50+50).min(_buf.len()); let _cl = _src.len().min(_end-50); _buf[50..50+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.people_1stname.as_bytes(); let _end = (100+50).min(_buf.len()); let _cl = _src.len().min(_end-100); _buf[100..100+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_stck_file.write_record(&_buf) {
        Ok(()) => state._fs_stck_file = FileStatus::Success,
        Err(e) => state._fs_stck_file = e,
    }
}

/// CLOSE STCK-FILE
fn stck_file_close(state: &mut ProgramState) {
    match state._fh_stck_file.close() {
        Ok(()) => state._fs_stck_file = FileStatus::Success,
        Err(e) => state._fs_stck_file = e,
    }
    state.stck_file_status = format!("{}", state._fs_stck_file).cobol_into();
}

/// OPEN INPUT LOCAL-FILE
fn local_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
}

/// OPEN OUTPUT LOCAL-FILE
fn local_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
}

/// OPEN I-O LOCAL-FILE
fn local_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
}

/// OPEN EXTEND LOCAL-FILE
fn local_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("LOCAL_FILE").unwrap_or("local_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_local_file = handle; state._fs_local_file = FileStatus::Success; }
        Err(_) => { state._fs_local_file = FileStatus::Error("35".to_string()); }
    }
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
}

/// READ LOCAL-FILE
fn local_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_local_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+50); state.lpeople_lastname = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            if _len > 50 { let _end = _len.min(50+50); state.lpeople_firstname = String::from_utf8_lossy(&_bytes[50.._end]).to_string().cobol_into(); }
            state._fs_local_file = FileStatus::Success;
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
        }
        Err(FileStatus::AtEnd) => {
            state._fs_local_file = FileStatus::AtEnd;
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
        }
        Err(e) => {
            state._fs_local_file = e.clone();
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
        }
    }
    Ok(())
}

/// WRITE to LOCAL-FILE
fn local_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.lpeople_lastname.as_bytes(); let _end = (0+50).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.lpeople_firstname.as_bytes(); let _end = (50+50).min(_buf.len()); let _cl = _src.len().min(_end-50); _buf[50..50+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_local_file.write_record(&_buf) {
        Ok(()) => state._fs_local_file = FileStatus::Success,
        Err(e) => state._fs_local_file = e,
    }
}

/// CLOSE LOCAL-FILE
fn local_file_close(state: &mut ProgramState) {
    match state._fh_local_file.close() {
        Ok(()) => state._fs_local_file = FileStatus::Success,
        Err(e) => state._fs_local_file = e,
    }
    state.locl_file_status = format!("{}", state._fs_local_file).cobol_into();
}

/// Paragraph: _IMPLICIT_
fn p__implicit_(state: &mut ProgramState) {
    // MAIN SECTION
    local_file_open_output(state);
    state.lpeople_firstname = format!("{}", "John").cobol_into();
    state.lpeople_lastname = format!("{}", "Doe").cobol_into();
    local_file_write(state);
    local_file_close(state);
    stck_file_open_output(state);
    state.people_firstname = format!("{}", "John").cobol_into();
    state.people_lastname = format!("{}", "Doe").cobol_into();
    stck_file_write(state);
    stck_file_close(state);
    // CALL 'INNER-PROGRAM' USING 
    return;
    // PROGRAM-ID. INNER-PROGRAM
    // PROCEDURE DIVISION
    stck_file_open_input(state);
    state.people_firstname = format!("{}", "John").cobol_into();
    state.people_lastname = format!("{}", "Doe").cobol_into();
    let _ = stck_file_read(state);
    // NOT INVALID KEY DISPLAY PEOPLE-FIRSTNAME " " PEOPLE-LASTNAME INVALID KEY DISPLAY "NOT FOUND" END-READ INITIALIZE STCK-REC MOVE "John" TO PEOPLE-1STNAME READ STCK-FILE KEY PEOPLE-NAMEKEYFIRST NOT INVALID KEY DISPLAY PEOPLE-FIRSTNAME " " PEOPLE-LASTNAME INVALID KEY DISPLAY "NOT FOUND" END-READ CLOSE STCK-FILE OPEN INPUT LOCAL-FILE MOVE "John" TO LPEOPLE-FIRSTNAME MOVE "Doe" TO LPEOPLE-LASTNAME READ LOCAL-FILE KEY LPEOPLE-NAMEKEY NOT INVALID KEY DISPLAY LPEOPLE-FIRSTNAME " " LPEOPLE-LASTNAME INVALID KEY DISPLAY "NOT FOUND" END-READ CLOSE LOCAL-FILE GOBACK
    // END PROGRAM INNER-PROGRAM
    // END PROGRAM OUTER-PROGRAM
}

fn main() {
    let mut state = ProgramState::default();
    p__implicit_(&mut state);
}
