// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Source: PROG.cbl
// Do not edit manually. Regenerate from COBOL source.
#![allow(unused_imports, unused_variables, dead_code, unused_parens, non_snake_case, invalid_value)]

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
use cobol_runtime::string_ops::*;
use cobol_runtime::chrono_shim::*;
use cobol_runtime::CobolInto;
use cobol_runtime::cobol_helpers::*;
use cobol_runtime::define_record;

define_record! {
    /// MY-RECORD
    pub struct MyRecord {
        /// MY-RECORD-DATA
        pub my_record_data: FixedString<80>,
    }
}


pub struct ProgramState {
    // --- FD: MY-FILE ---
    /// FD: MY-RECORD (group)
    pub my_record: FixedString<80>,
    /// FD: MY-RECORD-DATA
    pub my_record_data: FixedString<80>,
    // --- File handles ---
    /// File status for MY-FILE
    pub _fs_my_file: FileStatus,
    /// File handle for MY-FILE
    pub _fh_my_file: CobolFile,
    // --- Special registers ---
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        unsafe { std::mem::MaybeUninit::zeroed().assume_init() }
    }
}

/// OPEN INPUT MY-FILE
fn my_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("MY_FILE").unwrap_or("my_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_my_file = handle; state._fs_my_file = FileStatus::Success; }
        Err(_) => { state._fs_my_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT MY-FILE
fn my_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("MY_FILE").unwrap_or("my_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_my_file = handle; state._fs_my_file = FileStatus::Success; }
        Err(_) => { state._fs_my_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN I-O MY-FILE
fn my_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("MY_FILE").unwrap_or("my_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_my_file = handle; state._fs_my_file = FileStatus::Success; }
        Err(_) => { state._fs_my_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND MY-FILE
fn my_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("MY_FILE").unwrap_or("my_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_my_file = handle; state._fs_my_file = FileStatus::Success; }
        Err(_) => { state._fs_my_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ MY-FILE
fn my_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_my_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(80);
            if _len > 0 { let _end = _len.min(0+80); state.my_record_data = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_my_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_my_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_my_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to MY-FILE
fn my_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 80];
    { let _src = state.my_record_data.as_bytes(); let _end = (0+80).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_my_file.write_record(&_buf) {
        Ok(()) => state._fs_my_file = FileStatus::Success,
        Err(e) => state._fs_my_file = e,
    }
}

/// CLOSE MY-FILE
fn my_file_close(state: &mut ProgramState) {
    match state._fh_my_file.close() {
        Ok(()) => state._fs_my_file = FileStatus::Success,
        Err(e) => state._fs_my_file = e,
    }
}

/// Paragraph: _IMPLICIT_
fn p__implicit_(state: &mut ProgramState) {
    return;
}

fn main() {
    let mut state = ProgramState::default();
    p__implicit_(&mut state);
}
