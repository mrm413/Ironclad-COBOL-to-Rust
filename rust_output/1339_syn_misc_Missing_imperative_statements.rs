// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Source: PROG.cbl
// Do not edit manually. Regenerate from COBOL source.
#![allow(unused_imports, unused_variables, dead_code, unused_parens, non_snake_case)]

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::string_ops::*;
use cobol_runtime::chrono_shim::*;
use cobol_runtime::CobolInto;
use cobol_runtime::cobol_helpers::*;
use cobol_runtime::define_record;

define_record! {
    /// C
    pub struct C {
    }
}


#[derive(Default)]
pub struct ProgramState {
    // --- WORKING-STORAGE SECTION ---
    /// WS: VAR
    pub var: FixedString<200>,
    /// WS: OTHERVAR
    pub othervar: FixedString<115>,
    /// WS: A
    pub a: u8,
    /// WS: B
    pub b: u8,
    /// WS: C (group)
    pub c: FixedString<1>,
    /// WS: VAR-LEN
    pub var_len: FixedString<1>,
    // --- Special registers ---
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}


/// Paragraph: _IMPLICIT_
fn p__implicit_(state: &mut ProgramState) {
    if format!("{}", state.var_len).trim().parse::<f64>().unwrap_or(0.0) < format!("{}", 16).trim().parse::<f64>().unwrap_or(0.0) {
    } else {
        state.var = format!("{}", state.othervar).cobol_into();
    }
    if format!("{}", state.var_len).trim().parse::<f64>().unwrap_or(0.0) > format!("{}", 16).trim().parse::<f64>().unwrap_or(0.0) {
    } else {
        state.var = format!("{}", state.othervar).cobol_into();
    }
    if format!("{}", 2).trim() == format!("{}", 3).trim() {
    } else {
        if format!("{}", 1).trim() == format!("{}", 1).trim() {
        } else {
            if format!("{}", 1).trim() == format!("{}", 1).trim() {
                state.othervar = format!("{}", state.var).cobol_into();
            }
        }
    }
    if ((format!("{}", state.a).trim() == format!("{}", state.b).trim())) {
        state.othervar = format!("{}", state.var).cobol_into();
    } else {
    }
    p_with(state);
    // TEST BEFORE UNTIL 1 <> 3 END-PERFORM EVALUATE FALSE WHEN A = B WHEN B = A END-EVALUATE
    if ((format!("{}", state.a).trim() == format!("{}", state.b).trim())) {
        state.othervar = format!("{}", state.var).cobol_into();
    } else {
    }
    if ((format!("{}", state.a).trim() == format!("{}", state.b).trim())) {
    } else if ((format!("{}", state.b).trim() == format!("{}", state.a).trim())) {
    }
    std::process::exit(0);
}

/// Stub for unresolved paragraph
fn p_with(state: &mut ProgramState) {
    // TODO: paragraph not parsed — stub
}

fn main() {
    let mut state = ProgramState::default();
    p__implicit_(&mut state);
}
