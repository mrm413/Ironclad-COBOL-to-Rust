// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct Srtfil {
    /// SRTREC
    pub srtrec: FixedString<256>,
}
impl std::fmt::Display for Srtfil {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.srtrec)
    }
}
impl From<&str> for Srtfil {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Srtfil {
    fn from(_s: String) -> Self { Self::default() }
}
impl Srtfil {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Stfile {
    /// STFREC
    pub stfrec: FixedString<256>,
}
impl std::fmt::Display for Stfile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.stfrec)
    }
}
impl From<&str> for Stfile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Stfile {
    fn from(_s: String) -> Self { Self::default() }
}
impl Stfile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct G {
    /// TBL
    pub tbl: [Tbl; 5],
    /// TBL-ORD
    pub tbl_ord: [TblOrd; 5],
}
impl std::fmt::Display for G {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "")
    }
}
impl From<&str> for G {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for G {
    fn from(_s: String) -> Self { Self::default() }
}
impl G {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Tbl {
    /// X
    pub x: FixedString<1>,
    /// Y
    pub y: u32,
}
impl std::fmt::Display for Tbl {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.x, self.y)
    }
}
impl From<&str> for Tbl {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Tbl {
    fn from(_s: String) -> Self { Self::default() }
}
impl Tbl {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct TblOrd {
    /// XO
    pub xo: FixedString<1>,
    /// YO
    pub yo: u32,
}
impl std::fmt::Display for TblOrd {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.xo, self.yo)
    }
}
impl From<&str> for TblOrd {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for TblOrd {
    fn from(_s: String) -> Self { Self::default() }
}
impl TblOrd {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: SRTREC
    pub srtrec: FixedString<256>,
    /// FD: STFREC
    pub stfrec: FixedString<256>,
    /// G
    pub g: G,
    /// TBL
    pub tbl: [Tbl; 5],
    /// X
    pub x: FixedString<1>,
    /// Y
    pub y: u32,
    /// TBL-ORD
    pub tbl_ord: [TblOrd; 5],
    /// XO
    pub xo: FixedString<1>,
    /// YO
    pub yo: u32,
    /// File status for SRTFIL
    pub _fs_srtfil: FileStatus,
    /// File handle for SRTFIL
    pub _fh_srtfil: CobolFile,
    /// File status for STFILE
    pub _fs_stfile: FileStatus,
    /// File handle for STFILE
    pub _fh_stfile: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            srtrec: Default::default(),
            stfrec: Default::default(),
            g: Default::default(),
            tbl: Default::default(),
            x: Default::default(),
            y: Default::default(),
            tbl_ord: Default::default(),
            xo: Default::default(),
            yo: Default::default(),
            _fs_srtfil: FileStatus::Success,
            _fh_srtfil: CobolFile::Closed,
            _fs_stfile: FileStatus::Success,
            _fh_stfile: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// X
fn p_x(state: &mut ProgramState) {
    // // SORT TBL ON 
    // // SORT TBL ON 
    // // SORT TBL-ORD ON 
    // // SORT TBL-ORD ON 
    // // SORT STFILE ON DESC SRTREC
    // // SORT STFILE ON DESC SRTREC USING SRTFIL GIVING SRTFIL
    // // SORT STFILE ON 
    // // SORT STFILE ON 
    { let _ = state._fh_srtfil.close();
        let _ = state._fh_stfile.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT SRTFIL
fn srtfil_open_input(state: &mut ProgramState) {
    let path = std::env::var("SRTFIL").unwrap_or("srtfil.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_srtfil = handle; state._fs_srtfil = FileStatus::Success; }
        Err(_) => { state._fs_srtfil = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT SRTFIL
fn srtfil_open_output(state: &mut ProgramState) {
    let path = std::env::var("SRTFIL").unwrap_or("srtfil.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_srtfil = handle; state._fs_srtfil = FileStatus::Success; }
        Err(_) => { state._fs_srtfil = FileStatus::Error("35".to_string()); }
    }
}

/// READ SRTFIL
fn srtfil_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_srtfil.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(256);
            if _len > 0 { let _end = _len.min(0+256); state.srtrec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_srtfil = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_srtfil = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_srtfil = e.clone();
        }
    }
    Ok(())
}

/// WRITE to SRTFIL
fn srtfil_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 256];
    { let _src = state.srtrec.as_bytes(); let _end = (0+256).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_srtfil.write_record(&_buf) {
        Ok(()) => state._fs_srtfil = FileStatus::Success,
        Err(e) => state._fs_srtfil = e,
    }
}

/// CLOSE SRTFIL
fn srtfil_close(state: &mut ProgramState) {
    match state._fh_srtfil.close() {
        Ok(()) => state._fs_srtfil = FileStatus::Success,
        Err(e) => state._fs_srtfil = e,
    }
}

/// OPEN I-O SRTFIL
fn srtfil_open_io(state: &mut ProgramState) {
    let path = std::env::var("SRTFIL").unwrap_or("srtfil.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_srtfil = handle; state._fs_srtfil = FileStatus::Success; }
        Err(_) => { state._fs_srtfil = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND SRTFIL
fn srtfil_open_extend(state: &mut ProgramState) {
    let path = std::env::var("SRTFIL").unwrap_or("srtfil.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_srtfil = handle; state._fs_srtfil = FileStatus::Success; }
        Err(_) => { state._fs_srtfil = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT STFILE
fn stfile_open_input(state: &mut ProgramState) {
    let path = std::env::var("STFILE").unwrap_or("stfile.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_stfile = handle; state._fs_stfile = FileStatus::Success; }
        Err(_) => { state._fs_stfile = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT STFILE
fn stfile_open_output(state: &mut ProgramState) {
    let path = std::env::var("STFILE").unwrap_or("stfile.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_stfile = handle; state._fs_stfile = FileStatus::Success; }
        Err(_) => { state._fs_stfile = FileStatus::Error("35".to_string()); }
    }
}

/// READ STFILE
fn stfile_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_stfile.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(256);
            if _len > 0 { let _end = _len.min(0+256); state.stfrec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_stfile = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_stfile = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_stfile = e.clone();
        }
    }
    Ok(())
}

/// WRITE to STFILE
fn stfile_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 256];
    { let _src = state.stfrec.as_bytes(); let _end = (0+256).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_stfile.write_record(&_buf) {
        Ok(()) => state._fs_stfile = FileStatus::Success,
        Err(e) => state._fs_stfile = e,
    }
}

/// CLOSE STFILE
fn stfile_close(state: &mut ProgramState) {
    match state._fh_stfile.close() {
        Ok(()) => state._fs_stfile = FileStatus::Success,
        Err(e) => state._fs_stfile = e,
    }
}

/// OPEN I-O STFILE
fn stfile_open_io(state: &mut ProgramState) {
    let path = std::env::var("STFILE").unwrap_or("stfile.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_stfile = handle; state._fs_stfile = FileStatus::Success; }
        Err(_) => { state._fs_stfile = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND STFILE
fn stfile_open_extend(state: &mut ProgramState) {
    let path = std::env::var("STFILE").unwrap_or("stfile.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_stfile = handle; state._fs_stfile = FileStatus::Success; }
        Err(_) => { state._fs_stfile = FileStatus::Error("35".to_string()); }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_x(&mut state);
}

