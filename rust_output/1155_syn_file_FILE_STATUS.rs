// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Source: PROG.cbl
// Do not edit manually. Regenerate from COBOL source.
#![allow(unused_imports, unused_variables, dead_code, unused_parens, non_snake_case, invalid_value)]

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
use cobol_runtime::string_ops::*;
use cobol_runtime::chrono_shim::*;
use cobol_runtime::CobolInto;
use cobol_runtime::cobol_helpers::*;
use cobol_runtime::define_record;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct X {
    /// Y
    pub y: Decimal,
    /// Z
    pub z: [FixedString<2>; 1],
    /// OS
    pub os: FixedString<2>,
    /// Z2
    pub z2: [FixedString<2>; 1],
}
impl std::fmt::Display for X {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{:?}{}{:?}", self.y, self.z, self.os, self.z2)
    }
}
impl From<&str> for X {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for X {
    fn from(_s: String) -> Self { Self::default() }
}
impl X {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}


pub struct ProgramState {
    // --- FD: F ---
    /// FD: HS
    pub hs: FixedString<2>,
    // --- FD: G ---
    /// FD: G-REC
    pub g_rec: FixedString<1>,
    // --- FD: H ---
    /// FD: H-REC
    pub h_rec: FixedString<1>,
    // --- FD: J ---
    /// FD: J-REC
    pub j_rec: FixedString<1>,
    // --- FD: K ---
    /// FD: K-REC
    pub k_rec: FixedString<1>,
    // --- FD: L ---
    /// FD: L-REC
    pub l_rec: FixedString<1>,
    // --- FD: M ---
    /// FD: M-REC
    pub m_rec: FixedString<1>,
    // --- FD: N ---
    /// FD: N-REC
    pub n_rec: FixedString<1>,
    // --- FD: O ---
    /// FD: O-REC
    pub o_rec: FixedString<1>,
    // --- FD: P ---
    /// FD: P-REC
    pub p_rec: FixedString<1>,
    // --- FD: Q ---
    /// FD: Q-REC
    pub q_rec: FixedString<1>,
    // --- FD: R ---
    /// FD: R-REC
    pub r_rec: FixedString<1>,
    // --- WORKING-STORAGE SECTION ---
    /// WS: GS
    pub gs: Decimal,
    /// WS: JS
    pub js: Decimal,
    /// WS: KS
    pub ks: FixedString<2>,
    /// WS: LS
    pub ls: Decimal,
    /// WS: MS
    pub ms: Decimal,
    /// WS: NS
    pub ns: FixedString<3>,
    /// WS: X (group)
    pub x: FixedString<8>,
    /// WS: Y
    pub y: Decimal,
    /// WS: Z
    pub z: [FixedString<2>; 1],
    /// WS: OS
    pub os: FixedString<2>,
    /// WS: Z2
    pub z2: [FixedString<2>; 1],
    /// WS: RS
    pub rs: FixedString<1>,
    // --- File handles ---
    /// File status for F
    pub _fs_f: FileStatus,
    /// File handle for F
    pub _fh_f: CobolFile,
    /// File status for G
    pub _fs_g: FileStatus,
    /// File handle for G
    pub _fh_g: CobolFile,
    /// File status for H
    pub _fs_h: FileStatus,
    /// File handle for H
    pub _fh_h: CobolFile,
    /// File status for J
    pub _fs_j: FileStatus,
    /// File handle for J
    pub _fh_j: CobolFile,
    /// File status for K
    pub _fs_k: FileStatus,
    /// File handle for K
    pub _fh_k: CobolFile,
    /// File status for L
    pub _fs_l: FileStatus,
    /// File handle for L
    pub _fh_l: CobolFile,
    /// File status for M
    pub _fs_m: FileStatus,
    /// File handle for M
    pub _fh_m: CobolFile,
    /// File status for N
    pub _fs_n: FileStatus,
    /// File handle for N
    pub _fh_n: CobolFile,
    /// File status for O
    pub _fs_o: FileStatus,
    /// File handle for O
    pub _fh_o: CobolFile,
    /// File status for P
    pub _fs_p: FileStatus,
    /// File handle for P
    pub _fh_p: CobolFile,
    /// File status for Q
    pub _fs_q: FileStatus,
    /// File handle for Q
    pub _fh_q: CobolFile,
    /// File status for R
    pub _fs_r: FileStatus,
    /// File handle for R
    pub _fh_r: CobolFile,
    // --- Special registers ---
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        unsafe { std::mem::MaybeUninit::zeroed().assume_init() }
    }
}

impl ProgramState {
    pub fn ps(&self) -> bool { format!("{}", self.os).trim() == "00" }
}

/// OPEN INPUT F
fn f_open_input(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT F
fn f_open_output(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O F
fn f_open_io(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND F
fn f_open_extend(state: &mut ProgramState) {
    let path = std::env::var("F").unwrap_or("f.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_f = handle; state._fs_f = FileStatus::Success; }
        Err(_) => { state._fs_f = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ F
fn f_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_f.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(2);
            if _len > 0 { let _end = _len.min(0+2); state.hs = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_f = FileStatus::Success;
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_f = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_f = e.clone();
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to F
fn f_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 2];
    { let _src = state.hs.as_bytes(); let _end = (0+2).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_f.write_record(&_buf) {
        Ok(()) => state._fs_f = FileStatus::Success,
        Err(e) => state._fs_f = e,
    }
}

/// CLOSE F
fn f_close(state: &mut ProgramState) {
    match state._fh_f.close() {
        Ok(()) => state._fs_f = FileStatus::Success,
        Err(e) => state._fs_f = e,
    }
    { let _s = format!("{}", state._fs_f); let _b = _s.as_bytes(); unsafe { let p = &mut state.fs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT G
fn g_open_input(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT G
fn g_open_output(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O G
fn g_open_io(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND G
fn g_open_extend(state: &mut ProgramState) {
    let path = std::env::var("G").unwrap_or("g.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_g = handle; state._fs_g = FileStatus::Success; }
        Err(_) => { state._fs_g = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ G
fn g_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_g.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.g_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_g = FileStatus::Success;
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_g = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_g = e.clone();
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to G
fn g_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.g_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_g.write_record(&_buf) {
        Ok(()) => state._fs_g = FileStatus::Success,
        Err(e) => state._fs_g = e,
    }
}

/// CLOSE G
fn g_close(state: &mut ProgramState) {
    match state._fh_g.close() {
        Ok(()) => state._fs_g = FileStatus::Success,
        Err(e) => state._fs_g = e,
    }
    { let _s = format!("{}", state._fs_g); let _b = _s.as_bytes(); unsafe { let p = &mut state.gs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT H
fn h_open_input(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT H
fn h_open_output(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O H
fn h_open_io(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND H
fn h_open_extend(state: &mut ProgramState) {
    let path = std::env::var("H").unwrap_or("h.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_h = handle; state._fs_h = FileStatus::Success; }
        Err(_) => { state._fs_h = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ H
fn h_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_h.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.h_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_h = FileStatus::Success;
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_h = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_h = e.clone();
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to H
fn h_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.h_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_h.write_record(&_buf) {
        Ok(()) => state._fs_h = FileStatus::Success,
        Err(e) => state._fs_h = e,
    }
}

/// CLOSE H
fn h_close(state: &mut ProgramState) {
    match state._fh_h.close() {
        Ok(()) => state._fs_h = FileStatus::Success,
        Err(e) => state._fs_h = e,
    }
    { let _s = format!("{}", state._fs_h); let _b = _s.as_bytes(); unsafe { let p = &mut state.hs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT J
fn j_open_input(state: &mut ProgramState) {
    let path = std::env::var("J").unwrap_or("j.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_j = handle; state._fs_j = FileStatus::Success; }
        Err(_) => { state._fs_j = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT J
fn j_open_output(state: &mut ProgramState) {
    let path = std::env::var("J").unwrap_or("j.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_j = handle; state._fs_j = FileStatus::Success; }
        Err(_) => { state._fs_j = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O J
fn j_open_io(state: &mut ProgramState) {
    let path = std::env::var("J").unwrap_or("j.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_j = handle; state._fs_j = FileStatus::Success; }
        Err(_) => { state._fs_j = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND J
fn j_open_extend(state: &mut ProgramState) {
    let path = std::env::var("J").unwrap_or("j.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_j = handle; state._fs_j = FileStatus::Success; }
        Err(_) => { state._fs_j = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ J
fn j_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_j.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.j_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_j = FileStatus::Success;
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_j = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_j = e.clone();
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to J
fn j_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.j_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_j.write_record(&_buf) {
        Ok(()) => state._fs_j = FileStatus::Success,
        Err(e) => state._fs_j = e,
    }
}

/// CLOSE J
fn j_close(state: &mut ProgramState) {
    match state._fh_j.close() {
        Ok(()) => state._fs_j = FileStatus::Success,
        Err(e) => state._fs_j = e,
    }
    { let _s = format!("{}", state._fs_j); let _b = _s.as_bytes(); unsafe { let p = &mut state.js as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT K
fn k_open_input(state: &mut ProgramState) {
    let path = std::env::var("K").unwrap_or("k.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_k = handle; state._fs_k = FileStatus::Success; }
        Err(_) => { state._fs_k = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT K
fn k_open_output(state: &mut ProgramState) {
    let path = std::env::var("K").unwrap_or("k.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_k = handle; state._fs_k = FileStatus::Success; }
        Err(_) => { state._fs_k = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O K
fn k_open_io(state: &mut ProgramState) {
    let path = std::env::var("K").unwrap_or("k.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_k = handle; state._fs_k = FileStatus::Success; }
        Err(_) => { state._fs_k = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND K
fn k_open_extend(state: &mut ProgramState) {
    let path = std::env::var("K").unwrap_or("k.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_k = handle; state._fs_k = FileStatus::Success; }
        Err(_) => { state._fs_k = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ K
fn k_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_k.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.k_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_k = FileStatus::Success;
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_k = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_k = e.clone();
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to K
fn k_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.k_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_k.write_record(&_buf) {
        Ok(()) => state._fs_k = FileStatus::Success,
        Err(e) => state._fs_k = e,
    }
}

/// CLOSE K
fn k_close(state: &mut ProgramState) {
    match state._fh_k.close() {
        Ok(()) => state._fs_k = FileStatus::Success,
        Err(e) => state._fs_k = e,
    }
    { let _s = format!("{}", state._fs_k); let _b = _s.as_bytes(); unsafe { let p = &mut state.ks as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT L
fn l_open_input(state: &mut ProgramState) {
    let path = std::env::var("L").unwrap_or("l.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_l = handle; state._fs_l = FileStatus::Success; }
        Err(_) => { state._fs_l = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT L
fn l_open_output(state: &mut ProgramState) {
    let path = std::env::var("L").unwrap_or("l.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_l = handle; state._fs_l = FileStatus::Success; }
        Err(_) => { state._fs_l = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O L
fn l_open_io(state: &mut ProgramState) {
    let path = std::env::var("L").unwrap_or("l.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_l = handle; state._fs_l = FileStatus::Success; }
        Err(_) => { state._fs_l = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND L
fn l_open_extend(state: &mut ProgramState) {
    let path = std::env::var("L").unwrap_or("l.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_l = handle; state._fs_l = FileStatus::Success; }
        Err(_) => { state._fs_l = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ L
fn l_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_l.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.l_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_l = FileStatus::Success;
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_l = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_l = e.clone();
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to L
fn l_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.l_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_l.write_record(&_buf) {
        Ok(()) => state._fs_l = FileStatus::Success,
        Err(e) => state._fs_l = e,
    }
}

/// CLOSE L
fn l_close(state: &mut ProgramState) {
    match state._fh_l.close() {
        Ok(()) => state._fs_l = FileStatus::Success,
        Err(e) => state._fs_l = e,
    }
    { let _s = format!("{}", state._fs_l); let _b = _s.as_bytes(); unsafe { let p = &mut state.ls as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT M
fn m_open_input(state: &mut ProgramState) {
    let path = std::env::var("M").unwrap_or("m.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_m = handle; state._fs_m = FileStatus::Success; }
        Err(_) => { state._fs_m = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT M
fn m_open_output(state: &mut ProgramState) {
    let path = std::env::var("M").unwrap_or("m.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_m = handle; state._fs_m = FileStatus::Success; }
        Err(_) => { state._fs_m = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O M
fn m_open_io(state: &mut ProgramState) {
    let path = std::env::var("M").unwrap_or("m.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_m = handle; state._fs_m = FileStatus::Success; }
        Err(_) => { state._fs_m = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND M
fn m_open_extend(state: &mut ProgramState) {
    let path = std::env::var("M").unwrap_or("m.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_m = handle; state._fs_m = FileStatus::Success; }
        Err(_) => { state._fs_m = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ M
fn m_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_m.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.m_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_m = FileStatus::Success;
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_m = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_m = e.clone();
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to M
fn m_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.m_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_m.write_record(&_buf) {
        Ok(()) => state._fs_m = FileStatus::Success,
        Err(e) => state._fs_m = e,
    }
}

/// CLOSE M
fn m_close(state: &mut ProgramState) {
    match state._fh_m.close() {
        Ok(()) => state._fs_m = FileStatus::Success,
        Err(e) => state._fs_m = e,
    }
    { let _s = format!("{}", state._fs_m); let _b = _s.as_bytes(); unsafe { let p = &mut state.ms as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT N
fn n_open_input(state: &mut ProgramState) {
    let path = std::env::var("N").unwrap_or("n.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_n = handle; state._fs_n = FileStatus::Success; }
        Err(_) => { state._fs_n = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT N
fn n_open_output(state: &mut ProgramState) {
    let path = std::env::var("N").unwrap_or("n.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_n = handle; state._fs_n = FileStatus::Success; }
        Err(_) => { state._fs_n = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O N
fn n_open_io(state: &mut ProgramState) {
    let path = std::env::var("N").unwrap_or("n.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_n = handle; state._fs_n = FileStatus::Success; }
        Err(_) => { state._fs_n = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND N
fn n_open_extend(state: &mut ProgramState) {
    let path = std::env::var("N").unwrap_or("n.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_n = handle; state._fs_n = FileStatus::Success; }
        Err(_) => { state._fs_n = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ N
fn n_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_n.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.n_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_n = FileStatus::Success;
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_n = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_n = e.clone();
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to N
fn n_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.n_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_n.write_record(&_buf) {
        Ok(()) => state._fs_n = FileStatus::Success,
        Err(e) => state._fs_n = e,
    }
}

/// CLOSE N
fn n_close(state: &mut ProgramState) {
    match state._fh_n.close() {
        Ok(()) => state._fs_n = FileStatus::Success,
        Err(e) => state._fs_n = e,
    }
    { let _s = format!("{}", state._fs_n); let _b = _s.as_bytes(); unsafe { let p = &mut state.ns as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT O
fn o_open_input(state: &mut ProgramState) {
    let path = std::env::var("O").unwrap_or("o.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_o = handle; state._fs_o = FileStatus::Success; }
        Err(_) => { state._fs_o = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT O
fn o_open_output(state: &mut ProgramState) {
    let path = std::env::var("O").unwrap_or("o.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_o = handle; state._fs_o = FileStatus::Success; }
        Err(_) => { state._fs_o = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O O
fn o_open_io(state: &mut ProgramState) {
    let path = std::env::var("O").unwrap_or("o.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_o = handle; state._fs_o = FileStatus::Success; }
        Err(_) => { state._fs_o = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND O
fn o_open_extend(state: &mut ProgramState) {
    let path = std::env::var("O").unwrap_or("o.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_o = handle; state._fs_o = FileStatus::Success; }
        Err(_) => { state._fs_o = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ O
fn o_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_o.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.o_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_o = FileStatus::Success;
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_o = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_o = e.clone();
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to O
fn o_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.o_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_o.write_record(&_buf) {
        Ok(()) => state._fs_o = FileStatus::Success,
        Err(e) => state._fs_o = e,
    }
}

/// CLOSE O
fn o_close(state: &mut ProgramState) {
    match state._fh_o.close() {
        Ok(()) => state._fs_o = FileStatus::Success,
        Err(e) => state._fs_o = e,
    }
    { let _s = format!("{}", state._fs_o); let _b = _s.as_bytes(); unsafe { let p = &mut state.os as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT P
fn p_open_input(state: &mut ProgramState) {
    let path = std::env::var("P").unwrap_or("p.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_p = handle; state._fs_p = FileStatus::Success; }
        Err(_) => { state._fs_p = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT P
fn p_open_output(state: &mut ProgramState) {
    let path = std::env::var("P").unwrap_or("p.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_p = handle; state._fs_p = FileStatus::Success; }
        Err(_) => { state._fs_p = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O P
fn p_open_io(state: &mut ProgramState) {
    let path = std::env::var("P").unwrap_or("p.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_p = handle; state._fs_p = FileStatus::Success; }
        Err(_) => { state._fs_p = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND P
fn p_open_extend(state: &mut ProgramState) {
    let path = std::env::var("P").unwrap_or("p.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_p = handle; state._fs_p = FileStatus::Success; }
        Err(_) => { state._fs_p = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ P
fn p_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_p.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.p_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_p = FileStatus::Success;
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_p = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_p = e.clone();
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to P
fn p_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.p_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_p.write_record(&_buf) {
        Ok(()) => state._fs_p = FileStatus::Success,
        Err(e) => state._fs_p = e,
    }
}

/// CLOSE P
fn p_close(state: &mut ProgramState) {
    match state._fh_p.close() {
        Ok(()) => state._fs_p = FileStatus::Success,
        Err(e) => state._fs_p = e,
    }
    { let _s = format!("{}", state._fs_p); let _b = _s.as_bytes(); unsafe { let p = &mut state.ps as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT Q
fn q_open_input(state: &mut ProgramState) {
    let path = std::env::var("Q").unwrap_or("q.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_q = handle; state._fs_q = FileStatus::Success; }
        Err(_) => { state._fs_q = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT Q
fn q_open_output(state: &mut ProgramState) {
    let path = std::env::var("Q").unwrap_or("q.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_q = handle; state._fs_q = FileStatus::Success; }
        Err(_) => { state._fs_q = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O Q
fn q_open_io(state: &mut ProgramState) {
    let path = std::env::var("Q").unwrap_or("q.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_q = handle; state._fs_q = FileStatus::Success; }
        Err(_) => { state._fs_q = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND Q
fn q_open_extend(state: &mut ProgramState) {
    let path = std::env::var("Q").unwrap_or("q.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_q = handle; state._fs_q = FileStatus::Success; }
        Err(_) => { state._fs_q = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ Q
fn q_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_q.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.q_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_q = FileStatus::Success;
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_q = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_q = e.clone();
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to Q
fn q_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.q_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_q.write_record(&_buf) {
        Ok(()) => state._fs_q = FileStatus::Success,
        Err(e) => state._fs_q = e,
    }
}

/// CLOSE Q
fn q_close(state: &mut ProgramState) {
    match state._fh_q.close() {
        Ok(()) => state._fs_q = FileStatus::Success,
        Err(e) => state._fs_q = e,
    }
    { let _s = format!("{}", state._fs_q); let _b = _s.as_bytes(); unsafe { let p = &mut state.non_existent as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN INPUT R
fn r_open_input(state: &mut ProgramState) {
    let path = std::env::var("R").unwrap_or("r.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_r = handle; state._fs_r = FileStatus::Success; }
        Err(_) => { state._fs_r = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT R
fn r_open_output(state: &mut ProgramState) {
    let path = std::env::var("R").unwrap_or("r.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_r = handle; state._fs_r = FileStatus::Success; }
        Err(_) => { state._fs_r = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O R
fn r_open_io(state: &mut ProgramState) {
    let path = std::env::var("R").unwrap_or("r.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_r = handle; state._fs_r = FileStatus::Success; }
        Err(_) => { state._fs_r = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND R
fn r_open_extend(state: &mut ProgramState) {
    let path = std::env::var("R").unwrap_or("r.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_r = handle; state._fs_r = FileStatus::Success; }
        Err(_) => { state._fs_r = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ R
fn r_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_r.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(1);
            if _len > 0 { let _end = _len.min(0+1); state.r_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_r = FileStatus::Success;
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_r = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_r = e.clone();
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to R
fn r_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 1];
    { let _src = state.r_rec.as_bytes(); let _end = (0+1).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_r.write_record(&_buf) {
        Ok(()) => state._fs_r = FileStatus::Success,
        Err(e) => state._fs_r = e,
    }
}

/// CLOSE R
fn r_close(state: &mut ProgramState) {
    match state._fh_r.close() {
        Ok(()) => state._fs_r = FileStatus::Success,
        Err(e) => state._fs_r = e,
    }
    { let _s = format!("{}", state._fs_r); let _b = _s.as_bytes(); unsafe { let p = &mut state.rs as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// Paragraph: _IMPLICIT_
fn p__implicit_(state: &mut ProgramState) {
    // CONTINUE
    // END PROGRAM PROG
}

fn main() {
    let mut state = ProgramState::default();
    p__implicit_(&mut state);
}
