// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct File0 {
    /// F0REC
    pub f0rec: FixedString<80>,
}
impl std::fmt::Display for File0 {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.f0rec)
    }
}
impl From<&str> for File0 {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for File0 {
    fn from(_s: String) -> Self { Self::default() }
}
impl File0 {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct FileOpt {
    /// FOREC
    pub forec: FixedString<80>,
}
impl std::fmt::Display for FileOpt {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.forec)
    }
}
impl From<&str> for FileOpt {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for FileOpt {
    fn from(_s: String) -> Self { Self::default() }
}
impl FileOpt {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: F0REC
    pub f0rec: FixedString<80>,
    /// FD: FOREC
    pub forec: FixedString<80>,
    /// WSFS
    pub wsfs: FixedString<2>,
    /// File status for FILE0
    pub _fs_file0: FileStatus,
    /// File handle for FILE0
    pub _fh_file0: CobolFile,
    /// File status for FILE-OPT
    pub _fs_file_opt: FileStatus,
    /// File handle for FILE-OPT
    pub _fh_file_opt: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            f0rec: Default::default(),
            forec: Default::default(),
            wsfs: Default::default(),
            _fs_file0: FileStatus::Success,
            _fh_file0: CobolFile::Closed,
            _fs_file_opt: FileStatus::Success,
            _fh_file_opt: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// WSFS
fn p_wsfs(state: &mut ProgramState) {
    state.forec = FixedString::from_str(&format!("{}", "A"));
    forec_write(state);
    if &format!("{}", state.wsfs) != &"00".to_string() {
        println!("{}{}", "STATUS WRITE A:", state.wsfs);
        file_opt_close(state);
        if &format!("{}", state.wsfs) != &"00".to_string() {
            println!("{}{}", "STATUS CLOSE:", state.wsfs);
            file0_open_extend(state);
            if &format!("{}", state.wsfs) != &"00".to_string() {
                println!("{}{}", "STATUS EXTEND, empty file:", state.wsfs);
                state.f0rec = FixedString::from_str(&format!("{}", "B"));
                f0rec_write(state);
                if &format!("{}", state.wsfs) != &"00".to_string() {
                    println!("{}{}", "STATUS WRITE B:", state.wsfs);
                    file0_close(state);
                    file_opt_open_input(state);
                    if &format!("{}", state.wsfs) != &"00".to_string() {
                        println!("{}{}", "STATUS INPUT:", state.wsfs);
                        file_opt_read(state);
                    } else {
                        if &format!("{}", state.wsfs) != &"00".to_string() {
                            println!("{}{}", "STATUS READ A:", state.wsfs);
                            file_opt_read(state);
                        } else {
                            if &format!("{}", state.wsfs) != &"00".to_string() {
                                println!("{}{}", "STATUS READ B:", state.wsfs);
                                file_opt_close(state);
                                { let _ = state._fh_file0.close();
                                    let _ = state._fh_file_opt.close();
                                    std::process::exit(0); }
                            }
                        }
                    }
                }
            }
        }
    }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT FILE0
fn file0_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILE0").unwrap_or("file0.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_file0 = handle; state._fs_file0 = FileStatus::Success; }
        Err(_) => { state._fs_file0 = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
}

/// OPEN OUTPUT FILE0
fn file0_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILE0").unwrap_or("file0.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_file0 = handle; state._fs_file0 = FileStatus::Success; }
        Err(_) => { state._fs_file0 = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
}

/// READ FILE0
fn file0_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_file0.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(80);
            if _len > 0 { let _end = _len.min(0+80); state.f0rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_file0 = FileStatus::Success;
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
        }
        Err(FileStatus::AtEnd) => {
            state._fs_file0 = FileStatus::AtEnd;
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
        }
        Err(e) => {
            state._fs_file0 = e.clone();
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
        }
    }
    Ok(())
}

/// WRITE to FILE0
fn file0_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 80];
    { let _src = state.f0rec.as_bytes(); let _end = (0+80).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_file0.write_record(&_buf) {
        Ok(()) => state._fs_file0 = FileStatus::Success,
        Err(e) => state._fs_file0 = e,
    }
}

/// CLOSE FILE0
fn file0_close(state: &mut ProgramState) {
    match state._fh_file0.close() {
        Ok(()) => state._fs_file0 = FileStatus::Success,
        Err(e) => state._fs_file0 = e,
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
}

/// OPEN I-O FILE0
fn file0_open_io(state: &mut ProgramState) {
    let path = std::env::var("FILE0").unwrap_or("file0.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_file0 = handle; state._fs_file0 = FileStatus::Success; }
        Err(_) => { state._fs_file0 = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
}

/// OPEN EXTEND FILE0
fn file0_open_extend(state: &mut ProgramState) {
    let path = std::env::var("FILE0").unwrap_or("file0.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_file0 = handle; state._fs_file0 = FileStatus::Success; }
        Err(_) => { state._fs_file0 = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file0));
}

/// OPEN INPUT FILE-OPT
fn file_opt_open_input(state: &mut ProgramState) {
    let path = std::env::var("FILE_OPT").unwrap_or("file_opt.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_file_opt = handle; state._fs_file_opt = FileStatus::Success; }
        Err(_) => { state._fs_file_opt = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
}

/// OPEN OUTPUT FILE-OPT
fn file_opt_open_output(state: &mut ProgramState) {
    let path = std::env::var("FILE_OPT").unwrap_or("file_opt.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_file_opt = handle; state._fs_file_opt = FileStatus::Success; }
        Err(_) => { state._fs_file_opt = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
}

/// READ FILE-OPT
fn file_opt_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_file_opt.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(80);
            if _len > 0 { let _end = _len.min(0+80); state.forec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_file_opt = FileStatus::Success;
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
        }
        Err(FileStatus::AtEnd) => {
            state._fs_file_opt = FileStatus::AtEnd;
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
        }
        Err(e) => {
            state._fs_file_opt = e.clone();
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
        }
    }
    Ok(())
}

/// WRITE to FILE-OPT
fn file_opt_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 80];
    { let _src = state.forec.as_bytes(); let _end = (0+80).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_file_opt.write_record(&_buf) {
        Ok(()) => state._fs_file_opt = FileStatus::Success,
        Err(e) => state._fs_file_opt = e,
    }
}

/// CLOSE FILE-OPT
fn file_opt_close(state: &mut ProgramState) {
    match state._fh_file_opt.close() {
        Ok(()) => state._fs_file_opt = FileStatus::Success,
        Err(e) => state._fs_file_opt = e,
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
}

/// OPEN I-O FILE-OPT
fn file_opt_open_io(state: &mut ProgramState) {
    let path = std::env::var("FILE_OPT").unwrap_or("file_opt.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_file_opt = handle; state._fs_file_opt = FileStatus::Success; }
        Err(_) => { state._fs_file_opt = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
}

/// OPEN EXTEND FILE-OPT
fn file_opt_open_extend(state: &mut ProgramState) {
    let path = std::env::var("FILE_OPT").unwrap_or("file_opt.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_file_opt = handle; state._fs_file_opt = FileStatus::Success; }
        Err(_) => { state._fs_file_opt = FileStatus::Error("35".to_string()); }
    }
    state.wsfs = FixedString::from(format!("{}", state._fs_file_opt));
}

/// WRITE F0REC
fn f0rec_write(state: &mut ProgramState) {
    // Write F0REC via file0_write (byte buffer)
    file0_write(state);
}

/// WRITE FOREC
fn forec_write(state: &mut ProgramState) {
    // Write FOREC via file_opt_write (byte buffer)
    file_opt_write(state);
}

fn main() {
    let mut state = ProgramState::default();
    p_wsfs(&mut state);
}

