// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct PipeIn {
    /// PIPE-MSG-IN
    pub pipe_msg_in: FixedString<132>,
}
impl std::fmt::Display for PipeIn {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.pipe_msg_in)
    }
}
impl From<&str> for PipeIn {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for PipeIn {
    fn from(_s: String) -> Self { Self::default() }
}
impl PipeIn {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct PipeOut {
    /// PIPE-MSG-OUT
    pub pipe_msg_out: FixedString<132>,
}
impl std::fmt::Display for PipeOut {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.pipe_msg_out)
    }
}
impl From<&str> for PipeOut {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for PipeOut {
    fn from(_s: String) -> Self { Self::default() }
}
impl PipeOut {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: PIPE-MSG-IN
    pub pipe_msg_in: FixedString<132>,
    /// FD: PIPE-MSG-OUT
    pub pipe_msg_out: FixedString<132>,
    /// F-STATUS
    pub f_status: FixedString<2>,
    /// W-COMMAND
    pub w_command: FixedString<100>,
    /// File status for PIPE-IN
    pub _fs_pipe_in: FileStatus,
    /// File handle for PIPE-IN
    pub _fh_pipe_in: CobolFile,
    /// File status for PIPE-OUT
    pub _fs_pipe_out: FileStatus,
    /// File handle for PIPE-OUT
    pub _fh_pipe_out: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            pipe_msg_in: Default::default(),
            pipe_msg_out: Default::default(),
            f_status: Default::default(),
            w_command: Default::default(),
            _fs_pipe_in: FileStatus::Success,
            _fh_pipe_in: CobolFile::Closed,
            _fs_pipe_out: FileStatus::Success,
            _fh_pipe_out: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// W-COMMAND
fn p_w_command(state: &mut ProgramState) {
    pipe_in_open_input(state);
    if !(state.f_status == "00") {
        println!("{}", "FAILED: OPEN INPUT");
        { let _ = state._fh_pipe_in.close();
            let _ = state._fh_pipe_out.close();
            std::process::exit(0); }
    }
    state.w_command = "> sh ./consumer > ./test-data-out".into();
    pipe_out_open_output(state);
    if !(state.f_status == "00") {
        println!("{}", "FAILED: OPEN OUTPUT");
        { let _ = state._fh_pipe_in.close();
            let _ = state._fh_pipe_out.close();
            std::process::exit(0); }
    }
    while !(!(state.f_status == "00")) {
        // SKIP: unresolved ref
    }
    pipe_in_close(state);
    pipe_out_close(state);
    { let _ = state._fh_pipe_in.close();
        let _ = state._fh_pipe_out.close();
        std::process::exit(0); }
}

/// X01-100-MAP
fn p_x01_100_map(state: &mut ProgramState) {
    state.pipe_msg_out = "*** Jacques Tati ***".into();
    // SKIP: unresolved condition
    state.pipe_msg_out = "Country: FRANCE".into();
    // SKIP: unresolved condition
    state.pipe_msg_out = "Town: DEAUVILLE".into();
    // SKIP: unresolved condition
    state.pipe_msg_out = "Name: M. Hulot".into();
    // SKIP: unresolved condition
    state.pipe_msg_out = "Street: Rue des Anglais".into();
}

/// STUB: p__unnamed
fn p__unnamed(state: &mut ProgramState) {
    // SKIP: unresolved ref
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT PIPE-IN
fn pipe_in_open_input(state: &mut ProgramState) {
    let path = std::env::var("PIPE_IN").unwrap_or("pipe_in.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_pipe_in = handle; state._fs_pipe_in = FileStatus::Success; }
        Err(_) => { state._fs_pipe_in = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT PIPE-IN
fn pipe_in_open_output(state: &mut ProgramState) {
    let path = std::env::var("PIPE_IN").unwrap_or("pipe_in.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_pipe_in = handle; state._fs_pipe_in = FileStatus::Success; }
        Err(_) => { state._fs_pipe_in = FileStatus::Error("35".to_string()); }
    }
}

/// READ PIPE-IN
fn pipe_in_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_pipe_in.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(132);
            if _len > 0 { let _end = _len.min(0+132); state.pipe_msg_in = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_pipe_in = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_pipe_in = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_pipe_in = e.clone();
        }
    }
    Ok(())
}

/// WRITE to PIPE-IN
fn pipe_in_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 132];
    { let _src = state.pipe_msg_in.as_bytes(); let _end = (0+132).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_pipe_in.write_record(&_buf) {
        Ok(()) => state._fs_pipe_in = FileStatus::Success,
        Err(e) => state._fs_pipe_in = e,
    }
}

/// CLOSE PIPE-IN
fn pipe_in_close(state: &mut ProgramState) {
    match state._fh_pipe_in.close() {
        Ok(()) => state._fs_pipe_in = FileStatus::Success,
        Err(e) => state._fs_pipe_in = e,
    }
}

/// OPEN I-O PIPE-IN
fn pipe_in_open_io(state: &mut ProgramState) {
    let path = std::env::var("PIPE_IN").unwrap_or("pipe_in.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_pipe_in = handle; state._fs_pipe_in = FileStatus::Success; }
        Err(_) => { state._fs_pipe_in = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND PIPE-IN
fn pipe_in_open_extend(state: &mut ProgramState) {
    let path = std::env::var("PIPE_IN").unwrap_or("pipe_in.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_pipe_in = handle; state._fs_pipe_in = FileStatus::Success; }
        Err(_) => { state._fs_pipe_in = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT PIPE-OUT
fn pipe_out_open_input(state: &mut ProgramState) {
    let path = std::env::var("PIPE_OUT").unwrap_or("pipe_out.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_pipe_out = handle; state._fs_pipe_out = FileStatus::Success; }
        Err(_) => { state._fs_pipe_out = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT PIPE-OUT
fn pipe_out_open_output(state: &mut ProgramState) {
    let path = std::env::var("PIPE_OUT").unwrap_or("pipe_out.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_pipe_out = handle; state._fs_pipe_out = FileStatus::Success; }
        Err(_) => { state._fs_pipe_out = FileStatus::Error("35".to_string()); }
    }
}

/// READ PIPE-OUT
fn pipe_out_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_pipe_out.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(132);
            if _len > 0 { let _end = _len.min(0+132); state.pipe_msg_out = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_pipe_out = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_pipe_out = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_pipe_out = e.clone();
        }
    }
    Ok(())
}

/// WRITE to PIPE-OUT
fn pipe_out_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 132];
    { let _src = state.pipe_msg_out.as_bytes(); let _end = (0+132).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_pipe_out.write_record(&_buf) {
        Ok(()) => state._fs_pipe_out = FileStatus::Success,
        Err(e) => state._fs_pipe_out = e,
    }
}

/// CLOSE PIPE-OUT
fn pipe_out_close(state: &mut ProgramState) {
    match state._fh_pipe_out.close() {
        Ok(()) => state._fs_pipe_out = FileStatus::Success,
        Err(e) => state._fs_pipe_out = e,
    }
}

/// OPEN I-O PIPE-OUT
fn pipe_out_open_io(state: &mut ProgramState) {
    let path = std::env::var("PIPE_OUT").unwrap_or("pipe_out.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_pipe_out = handle; state._fs_pipe_out = FileStatus::Success; }
        Err(_) => { state._fs_pipe_out = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND PIPE-OUT
fn pipe_out_open_extend(state: &mut ProgramState) {
    let path = std::env::var("PIPE_OUT").unwrap_or("pipe_out.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_pipe_out = handle; state._fs_pipe_out = FileStatus::Success; }
        Err(_) => { state._fs_pipe_out = FileStatus::Error("35".to_string()); }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_w_command(&mut state);
}

