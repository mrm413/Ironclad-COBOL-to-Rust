// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Source: PROG.cbl
// Do not edit manually. Regenerate from COBOL source.
#![allow(unused_imports, unused_variables, dead_code, unused_parens, non_snake_case)]

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
use cobol_runtime::string_ops::*;
use cobol_runtime::chrono_shim::*;
use cobol_runtime::CobolInto;
use cobol_runtime::cobol_helpers::*;
use cobol_runtime::define_record;

define_record! {
    /// CUST-ORDER-REC
    pub struct CustOrderRec {
        /// CUST-NUM
        pub cust_num: Decimal,
        /// ITEM-NUM
        pub item_num: Decimal,
        /// NUM-ORD
        pub num_ord: Decimal,
        /// PRICE
        pub price: Decimal,
        /// SHIPPING
        pub shipping: Decimal,
        /// FILLER-1
        pub filler_1: FixedString<7>,
    }
}

define_record! {
    /// INDICATORS
    pub struct Indicators {
        /// ARE-THERE-MORE-RECORDS
        pub are_there_more_records: FixedString<3>,
    }
}

define_record! {
    /// CONSTANTS
    pub struct Constants {
        /// SALES-TAX
        pub sales_tax: Decimal,
    }
}

define_record! {
    /// WORK-AREAS
    pub struct WorkAreas {
        /// AMT-TAX
        pub amt_tax: Decimal,
        /// AMT-ORDER
        pub amt_order: Decimal,
        /// TOT-ORDER
        pub tot_order: Decimal,
        /// CURRENT-TIME
        pub current_time: Decimal,
    }
}


#[derive(Default)]
pub struct ProgramState {
    // --- FD: CUST-ORDER-FILE ---
    /// FD: CUST-ORDER-REC (group)
    pub cust_order_rec: FixedString<30>,
    /// FD: CUST-NUM
    pub cust_num: Decimal,
    /// FD: ITEM-NUM
    pub item_num: Decimal,
    /// FD: NUM-ORD
    pub num_ord: Decimal,
    /// FD: PRICE
    pub price: Decimal,
    /// FD: SHIPPING
    pub shipping: Decimal,
    /// FD: FILLER
    pub filler_1: FixedString<7>,
    // --- FD: CUST-PRINT-FILE ---
    // --- WORKING-STORAGE SECTION ---
    /// WS: INDICATORS (group)
    pub indicators: FixedString<3>,
    /// WS: ARE-THERE-MORE-RECORDS
    pub are_there_more_records: FixedString<3>,
    /// WS: CONSTANTS (group)
    pub constants: FixedString<3>,
    /// WS: SALES-TAX
    pub sales_tax: Decimal,
    /// WS: WORK-AREAS (group)
    pub work_areas: FixedString<29>,
    /// WS: AMT-TAX
    pub amt_tax: Decimal,
    /// WS: AMT-ORDER
    pub amt_order: Decimal,
    /// WS: TOT-ORDER
    pub tot_order: Decimal,
    /// WS: CURRENT-TIME
    pub current_time: Decimal,
    // --- File handles ---
    /// File status for CUST-ORDER-FILE
    pub _fs_cust_order_file: FileStatus,
    /// File handle for CUST-ORDER-FILE
    pub _fh_cust_order_file: CobolFile,
    /// File status for CUST-PRINT-FILE
    pub _fs_cust_print_file: FileStatus,
    /// File handle for CUST-PRINT-FILE
    pub _fh_cust_print_file: CobolFile,
    // --- Special registers ---
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}


impl ProgramState {
    pub fn there_are_no_more_records(&self) -> bool { format!("{}", self.are_there_more_records).trim() == "NO " }
}

/// OPEN INPUT CUST-ORDER-FILE
fn cust_order_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("CUST_ORDER_FILE").unwrap_or("cust_order_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_cust_order_file = handle; state._fs_cust_order_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_order_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT CUST-ORDER-FILE
fn cust_order_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("CUST_ORDER_FILE").unwrap_or("cust_order_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_cust_order_file = handle; state._fs_cust_order_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_order_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN I-O CUST-ORDER-FILE
fn cust_order_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("CUST_ORDER_FILE").unwrap_or("cust_order_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_cust_order_file = handle; state._fs_cust_order_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_order_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND CUST-ORDER-FILE
fn cust_order_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("CUST_ORDER_FILE").unwrap_or("cust_order_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_cust_order_file = handle; state._fs_cust_order_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_order_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ CUST-ORDER-FILE
fn cust_order_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_cust_order_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(30);
            if _len > 0 { let _end = _len.min(0+5); let _s = String::from_utf8_lossy(&_bytes[0.._end]).trim().to_string(); state.cust_num = _s.cobol_into(); }
            if _len > 5 { let _end = _len.min(5+6); let _s = String::from_utf8_lossy(&_bytes[5.._end]).trim().to_string(); state.item_num = _s.cobol_into(); }
            if _len > 11 { let _end = _len.min(11+3); let _s = String::from_utf8_lossy(&_bytes[11.._end]).trim().to_string(); state.num_ord = _s.cobol_into(); }
            if _len > 14 { let _end = _len.min(14+5); let _s = String::from_utf8_lossy(&_bytes[14.._end]).trim().to_string(); state.price = _s.cobol_into(); }
            if _len > 19 { let _end = _len.min(19+4); let _s = String::from_utf8_lossy(&_bytes[19.._end]).trim().to_string(); state.shipping = _s.cobol_into(); }
            state._fs_cust_order_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_cust_order_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_cust_order_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to CUST-ORDER-FILE
fn cust_order_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 30];
    { let _s = format!("{:>width$}", state.cust_num, width = 5); let _b = _s.as_bytes(); let _cl = _b.len().min(5); _buf[0..0+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.item_num, width = 6); let _b = _s.as_bytes(); let _cl = _b.len().min(6); _buf[5..5+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.num_ord, width = 3); let _b = _s.as_bytes(); let _cl = _b.len().min(3); _buf[11..11+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.price, width = 5); let _b = _s.as_bytes(); let _cl = _b.len().min(5); _buf[14..14+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _s = format!("{:>width$}", state.shipping, width = 4); let _b = _s.as_bytes(); let _cl = _b.len().min(4); _buf[19..19+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    match state._fh_cust_order_file.write_record(&_buf) {
        Ok(()) => state._fs_cust_order_file = FileStatus::Success,
        Err(e) => state._fs_cust_order_file = e,
    }
}

/// CLOSE CUST-ORDER-FILE
fn cust_order_file_close(state: &mut ProgramState) {
    match state._fh_cust_order_file.close() {
        Ok(()) => state._fs_cust_order_file = FileStatus::Success,
        Err(e) => state._fs_cust_order_file = e,
    }
}

/// OPEN INPUT CUST-PRINT-FILE
fn cust_print_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("CUST_PRINT_FILE").unwrap_or("cust_print_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_cust_print_file = handle; state._fs_cust_print_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT CUST-PRINT-FILE
fn cust_print_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("CUST_PRINT_FILE").unwrap_or("cust_print_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_cust_print_file = handle; state._fs_cust_print_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN I-O CUST-PRINT-FILE
fn cust_print_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("CUST_PRINT_FILE").unwrap_or("cust_print_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_cust_print_file = handle; state._fs_cust_print_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND CUST-PRINT-FILE
fn cust_print_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("CUST_PRINT_FILE").unwrap_or("cust_print_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_cust_print_file = handle; state._fs_cust_print_file = FileStatus::Success; }
        Err(_) => { state._fs_cust_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ CUST-PRINT-FILE
fn cust_print_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_cust_print_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(0);
            state._fs_cust_print_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_cust_print_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_cust_print_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to CUST-PRINT-FILE
fn cust_print_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 0];
    match state._fh_cust_print_file.write_record(&_buf) {
        Ok(()) => state._fs_cust_print_file = FileStatus::Success,
        Err(e) => state._fs_cust_print_file = e,
    }
}

/// CLOSE CUST-PRINT-FILE
fn cust_print_file_close(state: &mut ProgramState) {
    match state._fh_cust_print_file.close() {
        Ok(()) => state._fs_cust_print_file = FileStatus::Success,
        Err(e) => state._fs_cust_print_file = e,
    }
}

/// Paragraph: A000-MAINLINE
fn p_a000_mainline(state: &mut ProgramState) {
    cust_order_file_open_input(state);
    // OUTPUT CUST-PRINT-FILE
    // INITIATE ORDER-REPORT
    let _ = cust_order_file_read(state);
    if state._fs_cust_order_file == FileStatus::AtEnd {
        state.are_there_more_records = format!("{}", "NO").cobol_into();
    }
    while !(state.there_are_no_more_records()) {
        p_a001_loop(state);
    }
    // TERMINATE ORDER-REPORT
    cust_order_file_close(state);
    cust_print_file_close(state);
    std::process::exit(0);
}

/// Paragraph: A001-LOOP
fn p_a001_loop(state: &mut ProgramState) {
    { let _a: f64 = format!("{}", state.price).trim().parse().unwrap_or(0.0); let _b: f64 = format!("{}", state.num_ord).trim().parse().unwrap_or(0.0); state.price = format!("{}", _a * _b).cobol_into(); }
    // GIVING AMT-ORDER
    { let _a: f64 = format!("{}", state.sales_tax).trim().parse().unwrap_or(0.0); let _b: f64 = format!("{}", state.amt_order).trim().parse().unwrap_or(0.0); state.sales_tax = format!("{}", _a * _b).cobol_into(); }
    // GIVING AMT-TAX
    { let _a: f64 = format!("{}", state.shipping).trim().parse().unwrap_or(0.0); let _b: f64 = format!("{}", state.amt_order).trim().parse().unwrap_or(0.0); state.shipping = format!("{}", _a + _b).cobol_into(); }
    // AMT-TAX GIVING TOT-ORDER
    // GENERATE DETAIL-LINE
    let _ = cust_order_file_read(state);
    if state._fs_cust_order_file == FileStatus::AtEnd {
        state.are_there_more_records = format!("{}", "NO").cobol_into();
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_a000_mainline(&mut state);
}
