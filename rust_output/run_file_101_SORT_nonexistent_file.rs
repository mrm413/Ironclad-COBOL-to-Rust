// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct SortIn {
    /// IN-REC
    pub in_rec: FixedString<100>,
}
impl std::fmt::Display for SortIn {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.in_rec)
    }
}
impl From<&str> for SortIn {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for SortIn {
    fn from(_s: String) -> Self { Self::default() }
}
impl SortIn {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct SortOut {
    /// OUT-REC
    pub out_rec: FixedString<100>,
}
impl std::fmt::Display for SortOut {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.out_rec)
    }
}
impl From<&str> for SortOut {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for SortOut {
    fn from(_s: String) -> Self { Self::default() }
}
impl SortOut {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct SortWrk {
    /// WRK-REC
    pub wrk_rec: FixedString<100>,
}
impl std::fmt::Display for SortWrk {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.wrk_rec)
    }
}
impl From<&str> for SortWrk {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for SortWrk {
    fn from(_s: String) -> Self { Self::default() }
}
impl SortWrk {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: IN-REC
    pub in_rec: FixedString<100>,
    /// FD: OUT-REC
    pub out_rec: FixedString<100>,
    /// FD: WRK-REC
    pub wrk_rec: FixedString<100>,
    /// File status for SORT-IN
    pub _fs_sort_in: FileStatus,
    /// File handle for SORT-IN
    pub _fh_sort_in: CobolFile,
    /// File status for SORT-OUT
    pub _fs_sort_out: FileStatus,
    /// File handle for SORT-OUT
    pub _fh_sort_out: CobolFile,
    /// File status for SORT-WRK
    pub _fs_sort_wrk: FileStatus,
    /// File handle for SORT-WRK
    pub _fh_sort_wrk: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            in_rec: Default::default(),
            out_rec: Default::default(),
            wrk_rec: Default::default(),
            _fs_sort_in: FileStatus::Success,
            _fh_sort_in: CobolFile::Closed,
            _fs_sort_out: FileStatus::Success,
            _fh_sort_out: CobolFile::Closed,
            _fs_sort_wrk: FileStatus::Success,
            _fh_sort_wrk: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// SORT-OUT
fn p_sort_out(state: &mut ProgramState) {
    { let _ = state._fh_sort_in.close();
        let _ = state._fh_sort_out.close();
        let _ = state._fh_sort_wrk.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT SORT-IN
fn sort_in_open_input(state: &mut ProgramState) {
    let path = std::env::var("SORT_IN").unwrap_or("sort_in.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_sort_in = handle; state._fs_sort_in = FileStatus::Success; }
        Err(_) => { state._fs_sort_in = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT SORT-IN
fn sort_in_open_output(state: &mut ProgramState) {
    let path = std::env::var("SORT_IN").unwrap_or("sort_in.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_sort_in = handle; state._fs_sort_in = FileStatus::Success; }
        Err(_) => { state._fs_sort_in = FileStatus::Error("35".to_string()); }
    }
}

/// READ SORT-IN
fn sort_in_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_sort_in.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+100); state.in_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_sort_in = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_sort_in = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_sort_in = e.clone();
        }
    }
    Ok(())
}

/// WRITE to SORT-IN
fn sort_in_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.in_rec.as_bytes(); let _end = (0+100).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_sort_in.write_record(&_buf) {
        Ok(()) => state._fs_sort_in = FileStatus::Success,
        Err(e) => state._fs_sort_in = e,
    }
}

/// CLOSE SORT-IN
fn sort_in_close(state: &mut ProgramState) {
    match state._fh_sort_in.close() {
        Ok(()) => state._fs_sort_in = FileStatus::Success,
        Err(e) => state._fs_sort_in = e,
    }
}

/// OPEN I-O SORT-IN
fn sort_in_open_io(state: &mut ProgramState) {
    let path = std::env::var("SORT_IN").unwrap_or("sort_in.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_sort_in = handle; state._fs_sort_in = FileStatus::Success; }
        Err(_) => { state._fs_sort_in = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND SORT-IN
fn sort_in_open_extend(state: &mut ProgramState) {
    let path = std::env::var("SORT_IN").unwrap_or("sort_in.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_sort_in = handle; state._fs_sort_in = FileStatus::Success; }
        Err(_) => { state._fs_sort_in = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT SORT-OUT
fn sort_out_open_input(state: &mut ProgramState) {
    let path = std::env::var("SORT_OUT").unwrap_or("sort_out.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_sort_out = handle; state._fs_sort_out = FileStatus::Success; }
        Err(_) => { state._fs_sort_out = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT SORT-OUT
fn sort_out_open_output(state: &mut ProgramState) {
    let path = std::env::var("SORT_OUT").unwrap_or("sort_out.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_sort_out = handle; state._fs_sort_out = FileStatus::Success; }
        Err(_) => { state._fs_sort_out = FileStatus::Error("35".to_string()); }
    }
}

/// READ SORT-OUT
fn sort_out_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_sort_out.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+100); state.out_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_sort_out = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_sort_out = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_sort_out = e.clone();
        }
    }
    Ok(())
}

/// WRITE to SORT-OUT
fn sort_out_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.out_rec.as_bytes(); let _end = (0+100).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_sort_out.write_record(&_buf) {
        Ok(()) => state._fs_sort_out = FileStatus::Success,
        Err(e) => state._fs_sort_out = e,
    }
}

/// CLOSE SORT-OUT
fn sort_out_close(state: &mut ProgramState) {
    match state._fh_sort_out.close() {
        Ok(()) => state._fs_sort_out = FileStatus::Success,
        Err(e) => state._fs_sort_out = e,
    }
}

/// OPEN I-O SORT-OUT
fn sort_out_open_io(state: &mut ProgramState) {
    let path = std::env::var("SORT_OUT").unwrap_or("sort_out.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_sort_out = handle; state._fs_sort_out = FileStatus::Success; }
        Err(_) => { state._fs_sort_out = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND SORT-OUT
fn sort_out_open_extend(state: &mut ProgramState) {
    let path = std::env::var("SORT_OUT").unwrap_or("sort_out.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_sort_out = handle; state._fs_sort_out = FileStatus::Success; }
        Err(_) => { state._fs_sort_out = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT SORT-WRK
fn sort_wrk_open_input(state: &mut ProgramState) {
    let path = std::env::var("SORT_WRK").unwrap_or("sort_wrk.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_sort_wrk = handle; state._fs_sort_wrk = FileStatus::Success; }
        Err(_) => { state._fs_sort_wrk = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT SORT-WRK
fn sort_wrk_open_output(state: &mut ProgramState) {
    let path = std::env::var("SORT_WRK").unwrap_or("sort_wrk.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_sort_wrk = handle; state._fs_sort_wrk = FileStatus::Success; }
        Err(_) => { state._fs_sort_wrk = FileStatus::Error("35".to_string()); }
    }
}

/// READ SORT-WRK
fn sort_wrk_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_sort_wrk.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+100); state.wrk_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_sort_wrk = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_sort_wrk = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_sort_wrk = e.clone();
        }
    }
    Ok(())
}

/// WRITE to SORT-WRK
fn sort_wrk_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.wrk_rec.as_bytes(); let _end = (0+100).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_sort_wrk.write_record(&_buf) {
        Ok(()) => state._fs_sort_wrk = FileStatus::Success,
        Err(e) => state._fs_sort_wrk = e,
    }
}

/// CLOSE SORT-WRK
fn sort_wrk_close(state: &mut ProgramState) {
    match state._fh_sort_wrk.close() {
        Ok(()) => state._fs_sort_wrk = FileStatus::Success,
        Err(e) => state._fs_sort_wrk = e,
    }
}

/// OPEN I-O SORT-WRK
fn sort_wrk_open_io(state: &mut ProgramState) {
    let path = std::env::var("SORT_WRK").unwrap_or("sort_wrk.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_sort_wrk = handle; state._fs_sort_wrk = FileStatus::Success; }
        Err(_) => { state._fs_sort_wrk = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND SORT-WRK
fn sort_wrk_open_extend(state: &mut ProgramState) {
    let path = std::env::var("SORT_WRK").unwrap_or("sort_wrk.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_sort_wrk = handle; state._fs_sort_wrk = FileStatus::Success; }
        Err(_) => { state._fs_sort_wrk = FileStatus::Error("35".to_string()); }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_sort_out(&mut state);
}

