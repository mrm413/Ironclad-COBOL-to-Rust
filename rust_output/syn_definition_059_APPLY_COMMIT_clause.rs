// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct StckRec {
    /// APPLY
    pub apply: FixedString<5>,
    /// STCK-QTY
    pub stck_qty: Decimal /* 5,2 Unsigned */,
}
impl std::fmt::Display for StckRec {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.apply, self.stck_qty)
    }
}
impl From<&str> for StckRec {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for StckRec {
    fn from(_s: String) -> Self { Self::default() }
}
impl StckRec {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct StckFile {
    /// STCK-REC
    pub stck_rec: StckRec,
}
impl std::fmt::Display for StckFile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.stck_rec)
    }
}
impl From<&str> for StckFile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for StckFile {
    fn from(_s: String) -> Self { Self::default() }
}
impl StckFile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct SortFile {
    /// SORT-REC
    pub sort_rec: FixedString<100>,
}
impl std::fmt::Display for SortFile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.sort_rec)
    }
}
impl From<&str> for SortFile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for SortFile {
    fn from(_s: String) -> Self { Self::default() }
}
impl SortFile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct ChngRec {
    /// CHNG-KEY
    pub chng_key: FixedString<5>,
    /// CHNG-QTY
    pub chng_qty: Decimal /* 5,2 Unsigned */,
    /// CHNG-ACTION
    pub chng_action: FixedString<1>,
    /// CHNG-STATE
    pub chng_state: FixedString<1>,
}
impl std::fmt::Display for ChngRec {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}{}{}", self.chng_key, self.chng_qty, self.chng_action, self.chng_state)
    }
}
impl From<&str> for ChngRec {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for ChngRec {
    fn from(_s: String) -> Self { Self::default() }
}
impl ChngRec {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct ChngFile {
    /// CHNG-REC
    pub chng_rec: ChngRec,
}
impl std::fmt::Display for ChngFile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.chng_rec)
    }
}
impl From<&str> for ChngFile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for ChngFile {
    fn from(_s: String) -> Self { Self::default() }
}
impl ChngFile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct FileStates {
    /// STCK-FILE-STATUS
    pub stck_file_status: FixedString<2>,
    /// CHNG-FILE-STATUS
    pub chng_file_status: ChngFileStatus,
}
impl std::fmt::Display for FileStates {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.stck_file_status, self.chng_file_status)
    }
}
impl From<&str> for FileStates {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for FileStates {
    fn from(_s: String) -> Self { Self::default() }
}
impl FileStates {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct ChngFileStatus {
    /// UPDATE-COUNT
    pub update_count: FixedString<1>,
    /// BASED-STUFF
    pub based_stuff: FixedString<1>,
}
impl std::fmt::Display for ChngFileStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}{}", self.update_count, self.based_stuff)
    }
}
impl From<&str> for ChngFileStatus {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for ChngFileStatus {
    fn from(_s: String) -> Self { Self::default() }
}
impl ChngFileStatus {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct DataHere {
    /// SOME-DATA
    pub some_data: u32,
}
impl std::fmt::Display for DataHere {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.some_data)
    }
}
impl From<&str> for DataHere {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for DataHere {
    fn from(_s: String) -> Self { Self::default() }
}
impl DataHere {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD group: STCK-REC
    pub stck_rec: FixedString<12>,
    /// FD: APPLY
    pub apply: FixedString<5>,
    /// FD: STCK-QTY
    pub stck_qty: Decimal /* 5,2 Unsigned */,
    /// FD: SORT-REC
    pub sort_rec: FixedString<100>,
    /// FD group: CHNG-REC
    pub chng_rec: FixedString<14>,
    /// FD: CHNG-KEY
    pub chng_key: FixedString<5>,
    /// FD: CHNG-QTY
    pub chng_qty: Decimal /* 5,2 Unsigned */,
    /// FD: CHNG-ACTION
    pub chng_action: FixedString<1>,
    /// FD: CHNG-STATE
    pub chng_state: FixedString<1>,
    /// FILE-STATES
    pub file_states: FileStates,
    /// STCK-FILE-STATUS
    pub stck_file_status: FixedString<2>,
    /// CHNG-FILE-STATUS
    pub chng_file_status: ChngFileStatus,
    /// UPDATE-COUNT
    pub update_count: FixedString<1>,
    /// BASED-STUFF
    pub based_stuff: FixedString<1>,
    /// DATA-HERE
    pub data_here: DataHere,
    /// SOME-DATA
    pub some_data: u32,
    /// RED-DATA
    pub red_data: FixedString<1>,
    /// File status for STCK-FILE
    pub _fs_stck_file: FileStatus,
    /// File handle for STCK-FILE
    pub _fh_stck_file: CobolFile,
    /// File status for SORT-FILE
    pub _fs_sort_file: FileStatus,
    /// File handle for SORT-FILE
    pub _fh_sort_file: CobolFile,
    /// File status for CHNG-FILE
    pub _fs_chng_file: FileStatus,
    /// File handle for CHNG-FILE
    pub _fh_chng_file: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            stck_rec: Default::default(),
            apply: Default::default(),
            stck_qty: Default::default(),
            sort_rec: Default::default(),
            chng_rec: Default::default(),
            chng_key: Default::default(),
            chng_qty: Default::default(),
            chng_action: Default::default(),
            chng_state: Default::default(),
            file_states: Default::default(),
            stck_file_status: Default::default(),
            chng_file_status: Default::default(),
            update_count: Default::default(),
            based_stuff: Default::default(),
            data_here: Default::default(),
            some_data: Default::default(),
            red_data: Default::default(),
            _fs_stck_file: FileStatus::Success,
            _fh_stck_file: CobolFile::Closed,
            _fs_sort_file: FileStatus::Success,
            _fh_sort_file: CobolFile::Closed,
            _fs_chng_file: FileStatus::Success,
            _fh_chng_file: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// SECTION
fn p_section(state: &mut ProgramState) {
    // PERFORM INITIALISATION — target not found, skipped
    // PERFORM TERMINATION — target not found, skipped
    { let _ = state._fh_stck_file.close();
        let _ = state._fh_sort_file.close();
        let _ = state._fh_chng_file.close();
        std::process::exit(0); }
}

/// SECTION
fn p_section_2(state: &mut ProgramState) {
    // SKIP: unresolved ref
    if true /* AUTO-FIX: E0308: mismatched types */ {
        // PERFORM FATAL-ERROR — target not found, skipped
    }
}

/// SECTION
fn p_section_3(state: &mut ProgramState) {
    if true /* AUTO-FIX: E0308: mismatched types */ {
        // PERFORM FATAL-ERROR — target not found, skipped
    }
}

/// SECTION
fn p_section_4(state: &mut ProgramState) {
    { let _ = state._fh_stck_file.close();
        let _ = state._fh_sort_file.close();
        let _ = state._fh_chng_file.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT STCK-FILE
fn stck_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN OUTPUT STCK-FILE
fn stck_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// READ STCK-FILE
fn stck_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_stck_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(12);
            if _len > 0 { let _end = _len.min(0+5); state.apply = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 5 { let _end = _len.min(5+7); let _s = String::from_utf8_lossy(&_bytes[5.._end]).trim().to_string(); state.stck_qty = _s.parse::<i64>().unwrap_or_default().into(); }
            state._fs_stck_file = FileStatus::Success;
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
        }
        Err(FileStatus::AtEnd) => {
            state._fs_stck_file = FileStatus::AtEnd;
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
        }
        Err(e) => {
            state._fs_stck_file = e.clone();
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
        }
    }
    Ok(())
}

/// WRITE to STCK-FILE
fn stck_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 12];
    { let _src = state.apply.as_bytes(); let _end = (0+5).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _s = format!("{:>width$}", state.stck_qty.value, width = 7); let _b = _s.as_bytes(); let _cl = _b.len().min(7); _buf[5..5+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    match state._fh_stck_file.write_record(&_buf) {
        Ok(()) => state._fs_stck_file = FileStatus::Success,
        Err(e) => state._fs_stck_file = e,
    }
}

/// CLOSE STCK-FILE
fn stck_file_close(state: &mut ProgramState) {
    match state._fh_stck_file.close() {
        Ok(()) => state._fs_stck_file = FileStatus::Success,
        Err(e) => state._fs_stck_file = e,
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN I-O STCK-FILE
fn stck_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN EXTEND STCK-FILE
fn stck_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("STCK_FILE").unwrap_or("stck_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_stck_file = handle; state._fs_stck_file = FileStatus::Success; }
        Err(_) => { state._fs_stck_file = FileStatus::Error("35".to_string()); }
    }
    state.stck_file_status = FixedString::from(format!("{}", state._fs_stck_file));
}

/// OPEN INPUT SORT-FILE
fn sort_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("SORT_FILE").unwrap_or("sort_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_sort_file = handle; state._fs_sort_file = FileStatus::Success; }
        Err(_) => { state._fs_sort_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT SORT-FILE
fn sort_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("SORT_FILE").unwrap_or("sort_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_sort_file = handle; state._fs_sort_file = FileStatus::Success; }
        Err(_) => { state._fs_sort_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ SORT-FILE
fn sort_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_sort_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(100);
            if _len > 0 { let _end = _len.min(0+100); state.sort_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_sort_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_sort_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_sort_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to SORT-FILE
fn sort_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 100];
    { let _src = state.sort_rec.as_bytes(); let _end = (0+100).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_sort_file.write_record(&_buf) {
        Ok(()) => state._fs_sort_file = FileStatus::Success,
        Err(e) => state._fs_sort_file = e,
    }
}

/// CLOSE SORT-FILE
fn sort_file_close(state: &mut ProgramState) {
    match state._fh_sort_file.close() {
        Ok(()) => state._fs_sort_file = FileStatus::Success,
        Err(e) => state._fs_sort_file = e,
    }
}

/// OPEN I-O SORT-FILE
fn sort_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("SORT_FILE").unwrap_or("sort_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_sort_file = handle; state._fs_sort_file = FileStatus::Success; }
        Err(_) => { state._fs_sort_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND SORT-FILE
fn sort_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("SORT_FILE").unwrap_or("sort_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_sort_file = handle; state._fs_sort_file = FileStatus::Success; }
        Err(_) => { state._fs_sort_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT CHNG-FILE
fn chng_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("CHNG_FILE").unwrap_or("chng_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_chng_file = handle; state._fs_chng_file = FileStatus::Success; }
        Err(_) => { state._fs_chng_file = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN OUTPUT CHNG-FILE
fn chng_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("CHNG_FILE").unwrap_or("chng_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_chng_file = handle; state._fs_chng_file = FileStatus::Success; }
        Err(_) => { state._fs_chng_file = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// READ CHNG-FILE
fn chng_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_chng_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(14);
            if _len > 0 { let _end = _len.min(0+5); state.chng_key = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            if _len > 5 { let _end = _len.min(5+7); let _s = String::from_utf8_lossy(&_bytes[5.._end]).trim().to_string(); state.chng_qty = _s.parse::<i64>().unwrap_or_default().into(); }
            if _len > 12 { let _end = _len.min(12+1); state.chng_action = FixedString::from(String::from_utf8_lossy(&_bytes[12.._end])); }
            if _len > 13 { let _end = _len.min(13+1); state.chng_state = FixedString::from(String::from_utf8_lossy(&_bytes[13.._end])); }
            state._fs_chng_file = FileStatus::Success;
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(FileStatus::AtEnd) => {
            state._fs_chng_file = FileStatus::AtEnd;
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
        Err(e) => {
            state._fs_chng_file = e.clone();
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
        }
    }
    Ok(())
}

/// WRITE to CHNG-FILE
fn chng_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 14];
    { let _src = state.chng_key.as_bytes(); let _end = (0+5).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    { let _s = format!("{:>width$}", state.chng_qty.value, width = 7); let _b = _s.as_bytes(); let _cl = _b.len().min(7); _buf[5..5+_cl].copy_from_slice(&_b[_b.len()-_cl..]); }
    { let _src = state.chng_action.as_bytes(); let _end = (12+1).min(_buf.len()); let _cl = _src.len().min(_end-12); _buf[12..12+_cl].copy_from_slice(&_src[.._cl]); }
    { let _src = state.chng_state.as_bytes(); let _end = (13+1).min(_buf.len()); let _cl = _src.len().min(_end-13); _buf[13..13+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_chng_file.write_record(&_buf) {
        Ok(()) => state._fs_chng_file = FileStatus::Success,
        Err(e) => state._fs_chng_file = e,
    }
}

/// CLOSE CHNG-FILE
fn chng_file_close(state: &mut ProgramState) {
    match state._fh_chng_file.close() {
        Ok(()) => state._fs_chng_file = FileStatus::Success,
        Err(e) => state._fs_chng_file = e,
    }
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN I-O CHNG-FILE
fn chng_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("CHNG_FILE").unwrap_or("chng_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_chng_file = handle; state._fs_chng_file = FileStatus::Success; }
        Err(_) => { state._fs_chng_file = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

/// OPEN EXTEND CHNG-FILE
fn chng_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("CHNG_FILE").unwrap_or("chng_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_chng_file = handle; state._fs_chng_file = FileStatus::Success; }
        Err(_) => { state._fs_chng_file = FileStatus::Error("35".to_string()); }
    }
    { let _s = format!("{}", state._fs_chng_file); let _b = _s.as_bytes(); unsafe { let p = &mut state.chng_file_status as *mut _ as *mut u8; for i in 0.._b.len().min(2) { *p.add(i) = _b[i]; } } }
}

fn main() {
    let mut state = ProgramState::default();
    p_section(&mut state);
}

