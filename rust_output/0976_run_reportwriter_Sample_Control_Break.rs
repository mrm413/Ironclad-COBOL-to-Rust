// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Source: PROG.cbl
// Do not edit manually. Regenerate from COBOL source.
#![allow(unused_imports, unused_variables, dead_code, unused_parens, non_snake_case)]

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
use cobol_runtime::string_ops::*;
use cobol_runtime::chrono_shim::*;
use cobol_runtime::CobolInto;
use cobol_runtime::cobol_helpers::*;
use cobol_runtime::define_record;

define_record! {
    /// INDICATORS
    pub struct Indicators {
        /// ARE-THERE-MORE-RECORDS
        pub are_there_more_records: FixedString<3>,
    }
}

define_record! {
    /// CONSTANTS
    pub struct Constants {
        /// NUM
        pub num: Decimal,
    }
}

define_record! {
    /// STUDENT-AREA
    pub struct StudentArea {
        /// STUDENT-NAME
        pub student_name: FixedString<20>,
        /// COURSE-PTS
        pub course_pts: Decimal,
        /// MAJOR
        pub major: FixedString<3>,
        /// ADVISOR
        pub advisor: FixedString<20>,
        /// CAMPUS
        pub campus: FixedString<15>,
    }
}


#[derive(Default)]
pub struct ProgramState {
    // --- FD: STUDENT-FILE ---
    /// FD: STUDENT-REC
    pub student_rec: FixedString<60>,
    // --- FD: PRINT-FILE ---
    // --- WORKING-STORAGE SECTION ---
    /// WS: INDICATORS (group)
    pub indicators: FixedString<3>,
    /// WS: ARE-THERE-MORE-RECORDS
    pub are_there_more_records: FixedString<3>,
    /// WS: CONSTANTS (group)
    pub constants: FixedString<2>,
    /// WS: NUM
    pub num: Decimal,
    /// WS: STUDENT-AREA (group)
    pub student_area: FixedString<60>,
    /// WS: STUDENT-NAME
    pub student_name: FixedString<20>,
    /// WS: COURSE-PTS
    pub course_pts: Decimal,
    /// WS: MAJOR
    pub major: FixedString<3>,
    /// WS: ADVISOR
    pub advisor: FixedString<20>,
    /// WS: CAMPUS
    pub campus: FixedString<15>,
    // --- File handles ---
    /// File status for STUDENT-FILE
    pub _fs_student_file: FileStatus,
    /// File handle for STUDENT-FILE
    pub _fh_student_file: CobolFile,
    /// File status for PRINT-FILE
    pub _fs_print_file: FileStatus,
    /// File handle for PRINT-FILE
    pub _fh_print_file: CobolFile,
    // --- Special registers ---
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}


impl ProgramState {
    pub fn there_are_no_more_records(&self) -> bool { format!("{}", self.are_there_more_records).trim() == "NO" }
}

/// OPEN INPUT STUDENT-FILE
fn student_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("STUDENT_FILE").unwrap_or("student_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_student_file = handle; state._fs_student_file = FileStatus::Success; }
        Err(_) => { state._fs_student_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT STUDENT-FILE
fn student_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("STUDENT_FILE").unwrap_or("student_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_student_file = handle; state._fs_student_file = FileStatus::Success; }
        Err(_) => { state._fs_student_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN I-O STUDENT-FILE
fn student_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("STUDENT_FILE").unwrap_or("student_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_student_file = handle; state._fs_student_file = FileStatus::Success; }
        Err(_) => { state._fs_student_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND STUDENT-FILE
fn student_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("STUDENT_FILE").unwrap_or("student_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_student_file = handle; state._fs_student_file = FileStatus::Success; }
        Err(_) => { state._fs_student_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ STUDENT-FILE
fn student_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_student_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(60);
            if _len > 0 { let _end = _len.min(0+60); state.student_rec = String::from_utf8_lossy(&_bytes[0.._end]).to_string().cobol_into(); }
            state._fs_student_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_student_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_student_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to STUDENT-FILE
fn student_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 60];
    { let _src = state.student_rec.as_bytes(); let _end = (0+60).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_student_file.write_record(&_buf) {
        Ok(()) => state._fs_student_file = FileStatus::Success,
        Err(e) => state._fs_student_file = e,
    }
}

/// CLOSE STUDENT-FILE
fn student_file_close(state: &mut ProgramState) {
    match state._fh_student_file.close() {
        Ok(()) => state._fs_student_file = FileStatus::Success,
        Err(e) => state._fs_student_file = e,
    }
}

/// OPEN INPUT PRINT-FILE
fn print_file_open_input(state: &mut ProgramState) {
    let path = std::env::var("PRINT_FILE").unwrap_or("print_file.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_print_file = handle; state._fs_print_file = FileStatus::Success; }
        Err(_) => { state._fs_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT PRINT-FILE
fn print_file_open_output(state: &mut ProgramState) {
    let path = std::env::var("PRINT_FILE").unwrap_or("print_file.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_print_file = handle; state._fs_print_file = FileStatus::Success; }
        Err(_) => { state._fs_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN I-O PRINT-FILE
fn print_file_open_io(state: &mut ProgramState) {
    let path = std::env::var("PRINT_FILE").unwrap_or("print_file.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_print_file = handle; state._fs_print_file = FileStatus::Success; }
        Err(_) => { state._fs_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND PRINT-FILE
fn print_file_open_extend(state: &mut ProgramState) {
    let path = std::env::var("PRINT_FILE").unwrap_or("print_file.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_print_file = handle; state._fs_print_file = FileStatus::Success; }
        Err(_) => { state._fs_print_file = FileStatus::Error("35".to_string()); }
    }
}

/// READ PRINT-FILE
fn print_file_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_print_file.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(0);
            state._fs_print_file = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_print_file = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_print_file = e.clone();
        }
    }
    Ok(())
}

/// WRITE to PRINT-FILE
fn print_file_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 0];
    match state._fh_print_file.write_record(&_buf) {
        Ok(()) => state._fs_print_file = FileStatus::Success,
        Err(e) => state._fs_print_file = e,
    }
}

/// CLOSE PRINT-FILE
fn print_file_close(state: &mut ProgramState) {
    match state._fh_print_file.close() {
        Ok(()) => state._fs_print_file = FileStatus::Success,
        Err(e) => state._fs_print_file = e,
    }
}

/// Paragraph: A000-CREATE-REPORTS
fn p_a000_create_reports(state: &mut ProgramState) {
    student_file_open_input(state);
    // OUTPUT PRINT-FILE
    // INITIATE CONTROL-BREAK
    let _ = student_file_read(state);
    if state._fs_student_file == FileStatus::AtEnd {
        state.are_there_more_records = format!("{}", "NO ").cobol_into();
    }
    while !(state.there_are_no_more_records()) {
        p_a001_loop(state);
    }
    // TERMINATE CONTROL-BREAK
    student_file_close(state);
    print_file_close(state);
    std::process::exit(0);
}

/// Paragraph: A001-LOOP
fn p_a001_loop(state: &mut ProgramState) {
    // GENERATE TRANS-LINE
    let _ = student_file_read(state);
    if state._fs_student_file == FileStatus::AtEnd {
        state.are_there_more_records = format!("{}", "NO ").cobol_into();
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_a000_create_reports(&mut state);
}
