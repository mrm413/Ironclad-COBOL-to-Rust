// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct Tstfil1 {
    /// TSTREC1
    pub tstrec1: FixedString<256>,
}
impl std::fmt::Display for Tstfil1 {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.tstrec1)
    }
}
impl From<&str> for Tstfil1 {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Tstfil1 {
    fn from(_s: String) -> Self { Self::default() }
}
impl Tstfil1 {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Tstfil2 {
    /// TSTREC2
    pub tstrec2: FixedString<256>,
}
impl std::fmt::Display for Tstfil2 {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.tstrec2)
    }
}
impl From<&str> for Tstfil2 {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Tstfil2 {
    fn from(_s: String) -> Self { Self::default() }
}
impl Tstfil2 {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: TSTREC1
    pub tstrec1: FixedString<256>,
    /// FD: TSTREC2
    pub tstrec2: FixedString<256>,
    /// OODB-RESULT
    pub oodb_result: FixedString<7>,
    /// File status for TSTFIL1
    pub _fs_tstfil1: FileStatus,
    /// File handle for TSTFIL1
    pub _fh_tstfil1: CobolFile,
    /// File status for TSTFIL2
    pub _fs_tstfil2: FileStatus,
    /// File handle for TSTFIL2
    pub _fh_tstfil2: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            tstrec1: Default::default(),
            tstrec2: Default::default(),
            oodb_result: Default::default(),
            _fs_tstfil1: FileStatus::Success,
            _fh_tstfil1: CobolFile::Closed,
            _fs_tstfil2: FileStatus::Success,
            _fh_tstfil2: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// DECLARATIVES
fn p_declaratives(state: &mut ProgramState) {
}

/// SECTION
fn p_section(state: &mut ProgramState) {
}

/// OODB-RESULT
fn p_oodb_result(state: &mut ProgramState) {
}

/// I-O-TEST-OODB-RESULT
fn p_i_o_test_oodb_result(state: &mut ProgramState) {
    if state.oodb_result == FixedString::spaces() {
        println!("{}", "LINE 1");
    } else {
        if true {
            println!("{}", "LINE 2");
        }
    }
    tstfil1_close(state);
    tstfil2_close(state);
}

/// DECLARATIVES
fn p_declaratives_2(state: &mut ProgramState) {
}

/// SECTION
fn p_section_2(state: &mut ProgramState) {
}

/// 000-BEGIN
fn p_000_begin(state: &mut ProgramState) {
    { let _ = state._fh_tstfil1.close();
        let _ = state._fh_tstfil2.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT TSTFIL1
fn tstfil1_open_input(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL1").unwrap_or("tstfil1.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_tstfil1 = handle; state._fs_tstfil1 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil1 = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT TSTFIL1
fn tstfil1_open_output(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL1").unwrap_or("tstfil1.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_tstfil1 = handle; state._fs_tstfil1 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil1 = FileStatus::Error("35".to_string()); }
    }
}

/// READ TSTFIL1
fn tstfil1_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_tstfil1.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(256);
            if _len > 0 { let _end = _len.min(0+256); state.tstrec1 = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_tstfil1 = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_tstfil1 = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_tstfil1 = e.clone();
        }
    }
    Ok(())
}

/// WRITE to TSTFIL1
fn tstfil1_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 256];
    { let _src = state.tstrec1.as_bytes(); let _end = (0+256).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_tstfil1.write_record(&_buf) {
        Ok(()) => state._fs_tstfil1 = FileStatus::Success,
        Err(e) => state._fs_tstfil1 = e,
    }
}

/// CLOSE TSTFIL1
fn tstfil1_close(state: &mut ProgramState) {
    match state._fh_tstfil1.close() {
        Ok(()) => state._fs_tstfil1 = FileStatus::Success,
        Err(e) => state._fs_tstfil1 = e,
    }
}

/// OPEN I-O TSTFIL1
fn tstfil1_open_io(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL1").unwrap_or("tstfil1.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_tstfil1 = handle; state._fs_tstfil1 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil1 = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND TSTFIL1
fn tstfil1_open_extend(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL1").unwrap_or("tstfil1.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_tstfil1 = handle; state._fs_tstfil1 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil1 = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN INPUT TSTFIL2
fn tstfil2_open_input(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL2").unwrap_or("tstfil2.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_tstfil2 = handle; state._fs_tstfil2 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil2 = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN OUTPUT TSTFIL2
fn tstfil2_open_output(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL2").unwrap_or("tstfil2.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_tstfil2 = handle; state._fs_tstfil2 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil2 = FileStatus::Error("35".to_string()); }
    }
}

/// READ TSTFIL2
fn tstfil2_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_tstfil2.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(256);
            if _len > 0 { let _end = _len.min(0+256); state.tstrec2 = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_tstfil2 = FileStatus::Success;
        }
        Err(FileStatus::AtEnd) => {
            state._fs_tstfil2 = FileStatus::AtEnd;
        }
        Err(e) => {
            state._fs_tstfil2 = e.clone();
        }
    }
    Ok(())
}

/// WRITE to TSTFIL2
fn tstfil2_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 256];
    { let _src = state.tstrec2.as_bytes(); let _end = (0+256).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_tstfil2.write_record(&_buf) {
        Ok(()) => state._fs_tstfil2 = FileStatus::Success,
        Err(e) => state._fs_tstfil2 = e,
    }
}

/// CLOSE TSTFIL2
fn tstfil2_close(state: &mut ProgramState) {
    match state._fh_tstfil2.close() {
        Ok(()) => state._fs_tstfil2 = FileStatus::Success,
        Err(e) => state._fs_tstfil2 = e,
    }
}

/// OPEN I-O TSTFIL2
fn tstfil2_open_io(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL2").unwrap_or("tstfil2.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_tstfil2 = handle; state._fs_tstfil2 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil2 = FileStatus::Error("35".to_string()); }
    }
}

/// OPEN EXTEND TSTFIL2
fn tstfil2_open_extend(state: &mut ProgramState) {
    let path = std::env::var("TSTFIL2").unwrap_or("tstfil2.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_tstfil2 = handle; state._fs_tstfil2 = FileStatus::Success; }
        Err(_) => { state._fs_tstfil2 = FileStatus::Error("35".to_string()); }
    }
}

fn main() {
    let mut state = ProgramState::default();
    p_declaratives(&mut state);
}

