// Generated by Ironclad — Deterministic COBOL-to-Rust Transpiler
// Do not edit manually. Regenerate from COBOL source.

use std::io::{BufRead, BufReader, BufWriter, Write};
use std::fs::File;

use cobol_runtime::FixedString;
use cobol_runtime::Decimal;
use cobol_runtime::PackedDecimal;
use cobol_runtime::FileStatus;
use cobol_runtime::CobolFile;
#[allow(unused_imports)]
use cobol_runtime::string_ops::*;
#[allow(unused_imports)]
use cobol_runtime::chrono_shim::*;


#[derive(Debug, Clone, Default, PartialEq)]
pub struct Infile {
    /// INPUT-REC
    pub input_rec: FixedString<40>,
}
impl std::fmt::Display for Infile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.input_rec)
    }
}
impl From<&str> for Infile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Infile {
    fn from(_s: String) -> Self { Self::default() }
}
impl Infile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Outfile {
    /// OUTPUT-REC
    pub output_rec: FixedString<40>,
}
impl std::fmt::Display for Outfile {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{}", self.output_rec)
    }
}
impl From<&str> for Outfile {
    fn from(_s: &str) -> Self { Self::default() }
}
impl From<String> for Outfile {
    fn from(_s: String) -> Self { Self::default() }
}
impl Outfile {
    pub fn as_str(&self) -> String { format!("{}", self) }
    pub fn as_bytes(&self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn as_bytes_mut(&mut self) -> Vec<u8> { format!("{}", self).into_bytes() }
    pub fn trimmed(&self) -> String { format!("{}", self).trim_end().to_string() }
}

#[derive(Debug, Clone)]
pub struct ProgramState {
    /// FD: INPUT-REC
    pub input_rec: FixedString<40>,
    /// FD: OUTPUT-REC
    pub output_rec: FixedString<40>,
    /// OUTPUT-FILE
    pub output_file: FixedString<19>,
    /// INPUT-FILE
    pub input_file: FixedString<19>,
    /// INPUT-STATUS
    pub input_status: FixedString<2>,
    /// INPUT-LEN
    pub input_len: u32,
    /// File status for INFILE
    pub _fs_infile: FileStatus,
    /// File handle for INFILE
    pub _fh_infile: CobolFile,
    /// File status for OUTFILE
    pub _fs_outfile: FileStatus,
    /// File handle for OUTFILE
    pub _fh_outfile: CobolFile,
    /// RETURN-CODE special register
    pub return_code: i32,
    /// TALLY special register
    pub tally: i32,
    /// SORT-RETURN special register
    pub sort_return: i32,
    /// COB-CRT-STATUS special register
    pub cob_crt_status: i32,
    /// DEBUG-ITEM special register
    pub debug_item: FixedString<80>,
    /// NUMBER-OF-CALL-PARAMETERS special register
    pub number_of_call_parameters: i32,
    /// WHEN-COMPILED special register
    pub when_compiled: FixedString<16>,
}

impl Default for ProgramState {
    fn default() -> Self {
        Self {
            input_rec: Default::default(),
            output_rec: Default::default(),
            output_file: FixedString::from_cobol_str("TEST-FILE"),
            input_file: FixedString::from_cobol_str("TEST-INP"),
            input_status: Default::default(),
            input_len: 0,
            _fs_infile: FileStatus::Success,
            _fh_infile: CobolFile::Closed,
            _fs_outfile: FileStatus::Success,
            _fh_outfile: CobolFile::Closed,
            return_code: 0,
            tally: 0,
            sort_return: 0,
            cob_crt_status: 0,
            debug_item: Default::default(),
            number_of_call_parameters: 0,
            when_compiled: Default::default(),
        }
    }
}

/// A000-BEGIN
fn p_a000_begin(state: &mut ProgramState) {
    infile_open_input(state);
    infile_read(state);
    println!("{}{}{}{}", " Read 1: STATUS IS ", state.input_status, " LENGTH IS ", state.input_len);
    println!("{}{}{}", "       :", state.input_rec, ":");
    infile_read(state);
    println!("{}{}{}{}", " Read 2: STATUS IS ", state.input_status, " LENGTH IS ", state.input_len);
    println!("{}{}{}", "       :", state.input_rec, ":");
    state.input_len = 0 as u32;
    infile_read(state);
    println!("{}{}{}{}", " Read 3: STATUS IS ", state.input_status, " LENGTH IS ", state.input_len);
    println!("{}{}{}", "       :", state.input_rec, ":");
    state.input_rec = FixedString::spaces();
    state.input_len = 0 as u32;
    infile_read(state);
    println!("{}{}{}{}", " Read 4: STATUS IS ", state.input_status, " LENGTH IS ", state.input_len);
    if { let _rm_b = state.input_status.as_bytes().to_vec(); String::from_utf8(_rm_b[(1 - 1) as usize..(1 - 1 + 1) as usize].to_vec()).unwrap_or_default() } == "0" {
        println!("{}{}{}", "       :", state.input_rec, ":");
    }
    infile_close(state);
    outfile_open_output(state);
    state.input_len = 9 as u32;
    state.output_rec = "Record 1.............".into();
    output_rec_write(state);
    println!("{}{}{}{}", "Write 1: STATUS IS ", state.input_status, " LENGTH IS ", state.input_len);
    state.input_len = 64 as u32;
    state.output_rec = FixedString::from_cobol_str(&format!("{}", "."));
    { let _s = (1 - 1) as usize; let _l = (8) as usize; let _sv = String::from("Record 2"); let _src = _sv.as_bytes(); let _dst = state.output_rec.as_bytes_mut(); let _cl = _l.min(_src.len()).min(_dst.len() - _s); _dst[_s.._s+_cl].copy_from_slice(&_src[.._cl]); }
    output_rec_write(state);
    println!("{}{}{}{}", "Write 2: STATUS IS ", state.input_status, " LENGTH IS ", state.input_len);
    outfile_close(state);
    { let _ = state._fh_infile.close();
        let _ = state._fh_outfile.close();
        std::process::exit(0); }
}

// ── Runtime Stubs ──────────────────────────────────────────────

/// OPEN INPUT INFILE
fn infile_open_input(state: &mut ProgramState) {
    let path = std::env::var("INFILE").unwrap_or("infile.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_infile = handle; state._fs_infile = FileStatus::Success; }
        Err(_) => { state._fs_infile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
}

/// OPEN OUTPUT INFILE
fn infile_open_output(state: &mut ProgramState) {
    let path = std::env::var("INFILE").unwrap_or("infile.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_infile = handle; state._fs_infile = FileStatus::Success; }
        Err(_) => { state._fs_infile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
}

/// READ INFILE
fn infile_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_infile.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(40);
            if _len > 0 { let _end = _len.min(0+40); state.input_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_infile = FileStatus::Success;
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
        }
        Err(FileStatus::AtEnd) => {
            state._fs_infile = FileStatus::AtEnd;
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
        }
        Err(e) => {
            state._fs_infile = e.clone();
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
        }
    }
    Ok(())
}

/// WRITE to INFILE
fn infile_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 40];
    { let _src = state.input_rec.as_bytes(); let _end = (0+40).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_infile.write_record(&_buf) {
        Ok(()) => state._fs_infile = FileStatus::Success,
        Err(e) => state._fs_infile = e,
    }
}

/// CLOSE INFILE
fn infile_close(state: &mut ProgramState) {
    match state._fh_infile.close() {
        Ok(()) => state._fs_infile = FileStatus::Success,
        Err(e) => state._fs_infile = e,
    }
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
}

/// OPEN I-O INFILE
fn infile_open_io(state: &mut ProgramState) {
    let path = std::env::var("INFILE").unwrap_or("infile.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_infile = handle; state._fs_infile = FileStatus::Success; }
        Err(_) => { state._fs_infile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
}

/// OPEN EXTEND INFILE
fn infile_open_extend(state: &mut ProgramState) {
    let path = std::env::var("INFILE").unwrap_or("infile.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_infile = handle; state._fs_infile = FileStatus::Success; }
        Err(_) => { state._fs_infile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_infile));
}

/// OPEN INPUT OUTFILE
fn outfile_open_input(state: &mut ProgramState) {
    let path = std::env::var("OUTFILE").unwrap_or("outfile.dat".to_string());
    match CobolFile::open_input(&path) {
        Ok(handle) => { state._fh_outfile = handle; state._fs_outfile = FileStatus::Success; }
        Err(_) => { state._fs_outfile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
}

/// OPEN OUTPUT OUTFILE
fn outfile_open_output(state: &mut ProgramState) {
    let path = std::env::var("OUTFILE").unwrap_or("outfile.dat".to_string());
    match CobolFile::open_output(&path) {
        Ok(handle) => { state._fh_outfile = handle; state._fs_outfile = FileStatus::Success; }
        Err(_) => { state._fs_outfile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
}

/// READ OUTFILE
fn outfile_read(state: &mut ProgramState) -> Result<(), FileStatus> {
    match state._fh_outfile.read_line() {
        Ok(line) => {
            let _bytes = line.as_bytes();
            let _len = _bytes.len().min(40);
            if _len > 0 { let _end = _len.min(0+40); state.output_rec = FixedString::from(String::from_utf8_lossy(&_bytes[0.._end])); }
            state._fs_outfile = FileStatus::Success;
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
        }
        Err(FileStatus::AtEnd) => {
            state._fs_outfile = FileStatus::AtEnd;
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
        }
        Err(e) => {
            state._fs_outfile = e.clone();
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
        }
    }
    Ok(())
}

/// WRITE to OUTFILE
fn outfile_write(state: &mut ProgramState) {
    let mut _buf = vec![b' '; 40];
    { let _src = state.output_rec.as_bytes(); let _end = (0+40).min(_buf.len()); let _cl = _src.len().min(_end-0); _buf[0..0+_cl].copy_from_slice(&_src[.._cl]); }
    match state._fh_outfile.write_record(&_buf) {
        Ok(()) => state._fs_outfile = FileStatus::Success,
        Err(e) => state._fs_outfile = e,
    }
}

/// CLOSE OUTFILE
fn outfile_close(state: &mut ProgramState) {
    match state._fh_outfile.close() {
        Ok(()) => state._fs_outfile = FileStatus::Success,
        Err(e) => state._fs_outfile = e,
    }
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
}

/// OPEN I-O OUTFILE
fn outfile_open_io(state: &mut ProgramState) {
    let path = std::env::var("OUTFILE").unwrap_or("outfile.dat".to_string());
    match CobolFile::open_io(&path) {
        Ok(handle) => { state._fh_outfile = handle; state._fs_outfile = FileStatus::Success; }
        Err(_) => { state._fs_outfile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
}

/// OPEN EXTEND OUTFILE
fn outfile_open_extend(state: &mut ProgramState) {
    let path = std::env::var("OUTFILE").unwrap_or("outfile.dat".to_string());
    match CobolFile::open_extend(&path) {
        Ok(handle) => { state._fh_outfile = handle; state._fs_outfile = FileStatus::Success; }
        Err(_) => { state._fs_outfile = FileStatus::Error("35".to_string()); }
    }
    state.input_status = FixedString::from(format!("{}", state._fs_outfile));
}

/// WRITE OUTPUT-REC
fn output_rec_write(state: &mut ProgramState) {
    // Write OUTPUT-REC via outfile_write (byte buffer)
    outfile_write(state);
}

fn main() {
    let mut state = ProgramState::default();
    p_a000_begin(&mut state);
}

